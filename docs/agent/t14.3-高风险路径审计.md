# T14.3 高风险路径审计（只读审计，未实施迁移）
> 任务：docs/agent/重构任务清单.md 状态表 T14.3「高风险控制流与持久化/容量路径」的**审计部分**；只读取证，未修改任何 Mixin/JSON/配置/loader/生产代码。证据基线：2026-08-08 工作副本；build/rfg/minecraft-src（Forge 14.23.5.2847 反编译树）；提交 e1db0e7（波次3）。交叉引用：注入语义以 docs/agent/t14.2-注入语义分类.md 为准；容量站点以 docs/agent/t2a-容量站点登记表.md 为准；守恒审计原以 T12.3（ConservationAuditor）为准，该审计器已随方案 A 移除（见 §3）。
> 状态：PARTIAL（审计材料已产出；§2/§3/§5 中 AE2 相关结论已按当前源码修正，验收仍待独立复核）

## 0. 结论摘要
1. **勘误**：t14.2 矩阵 E25（SlotItemHandlerMixin.replaceCompatibilityLimit/getSlotStackLimit）已不存在于当前工作副本——T3 收敛在 e1db0e7 删除该注入。**当前实测注入点 = 54 处（early 27 + late 27）**，不是原记的 50/49；L01/L03/L05 三处 AE2 注入亦已移除（方案 A 与死注入清理）。
2. **广告面与写入面一致性**：9 条容量/持久化路径中 8 条自洽；1 条依赖同源扩展的外部前置（E02/E03 CommandReplaceItem 朴素 setter 目标，t2a §2.5 已标断链风险）。
3. **simulate 与真实写入**：仅 E26（槽位广告）仍在热路径涉及；**L01（AE2 投喂）已无我方注入**，其 simulate 分支与分块守恒结论仅对测试/探针直接调用 `Ae2ItemHandlerInsertLimiter.insertCapped` 有效，不再是运行期路径。**remainder 守恒**：所有含 remainder 路径均满足 `storedDelta + remainderCount == offered`。
4. **高风险标注**：6 处 HEAD cancellable 完整接管（E06/E13/E16/E17/E18/E19）+ E12 构造中途锚点注入，均无可链式替代 → 保留；**E06/E12/E13/E16/E17/E18/E19 无行为测试**，迁移/验收前必须补测试门。
5. **无源码不可判定**：L18（Q-DESC-19）、L06（Q-DESC-13，反射禁令违规）、L07 字段面、E12/E13（OBS-06）；**原 L01（Q-DESC-08）条目已作废**（注入已移除，不再存在 descriptor 闭合问题）。
## 1. 逐路径审计矩阵
| 路径 | 注入形态 | 广告面 | 真实写入面 | simulate | remainder | 风险 | 迁移顺序 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| E04 EntityItemMerge | @Redirect / combineItems / getMaxStackSize()I（EntityItemMergeMixin.java:11-22；EntityItem.java:258 唯一调用点） | Math.max(candidate.max, current.max) 门控判定 | :260 门控 + :268 grow 合并落库、:271 setDead | 无 | 无 remainder 概念；门控失败不落库、成功全量并栈 | @Redirect 不可链式共存；门控与落库同源（自洽） | **M1**（迁移 @ModifyExpressionValue，证据已闭合） |
| E07 canMergeStacks | @Redirect / canMergeStacks / getInventoryStackLimit()I（InventoryPlayerAddResourceMixin.java:12-21；InventoryPlayer.java:69 唯一调用点） | resolveInventoryClampLimit(incoming, ...) | :67-69 门控合并判定 | 无 | 无（门控层） | P0 事实 c：resolveInventoryClampLimit 保持 2 个库存注入调用方 | **M2** |
| E08 addResource 取源 | @Redirect / addResource / getMaxStackSize()I ×2（:23-32；InventoryPlayer.java:341/:343） | source.getMaxStackSize() 取源统一 | :341/:343 夹取量 j → :354 grow 落库 | 无 | addResource 返回 i-j（vanilla 剩余量语义） | 空槽/非空槽可达路径等价已核 | **M2** |
| E09 addResource 夹取 | @Redirect / addResource / getInventoryStackLimit()I ×2（:34-43；InventoryPlayer.java:346/:348） | resolveInventoryClampLimit(source, ...) | 同 E08（夹取上界即落库上界） | 无 | 同 E08 | 写入面 = 广告面，自洽 | **M2** |
| E16 creative 校验 | @Inject HEAD cancellable 完整接管 processCreativeInventoryAction（NetHandlerPlayServerMixin.java:25-107；vanilla :1284-1330） | resolveCreativeStackLimit = stack.maxStackSize | putStackInSlot → Slot.putStack → setInventorySlotContents（朴素写入无夹取）；掉落面 dropItem → EntityItem | 无 | 无（非法包整体拒绝） | **高风险**：HEAD 接管无链式共存；写前仅校验不夹取——双端规则不一致时幽灵物品面；**无行为测试** | 保留（无链式替代）；测试门先行 |
| E12 ItemStackNbt 读 | @Inject / <init>(NBTTagCompound)V / FIELD itemDamage PUTFIELD AFTER（ItemStackNbtMixin.java:39-50；vanilla ItemStack.java:124-146） | 无（持久化面） | 构造中途覆写 stackSize = getInteger("Count")（vanilla :132 读 byte） | 无 | 无 | **高风险**：锚点依赖 itemDamage PUTFIELD 先于 stackSize PUTFIELD（已核 :132/:133）；OBS-06 shadow 无 refmap；**无行为测试** | 保留；运行期验证门 |
| E13 ItemStackNbt 写 | @Inject HEAD cancellable 完整接管 writeToNBT（:52-71；vanilla :245-266） | 无 | nbt.setInteger("Count", stackSize)——**TAG_Byte → TAG_Int schema 变化** | 无 | 无 | **高风险**：HEAD 接管 + schema 分歧（跨端 NBT 相等比较可能判不等）；**无行为测试** | 保留；测试门先行 |
| E17/E18 PacketBuffer | @Inject HEAD cancellable 完整接管 readItemStack/writeItemStack（PacketBufferMixin.java:16-59；vanilla :356-407） | 无（传输面） | id short + count(≤64 单字节；>64 魔数 -42=0xD6 + int32) + damage + compound（StackCountCodec.kt:9-23） | 无 | 无 | **高风险**：HEAD 接管无共存；>64 要求双端 mod；**无行为测试**（仅 StackCountCodecTest） | 保留；测试门先行 |
| E19 PacketUtil C→S | @Inject HEAD cancellable 完整接管 writeItemStackFromClientToServer（PacketUtilMixin.java:16-39；vanilla PacketUtil.java:38-62） | 无（传输面） | 同 E17 编码；NBT 用 getTagCompound 全量 | 无 | 无 | HEAD 接管无共存；**无行为测试** | 保留；测试门先行 |
| E26 SlotItemHandler | @ModifyReturnValue / getItemStackLimit（SlotItemHandlerMixin.java:31-38） | 广告值 = handler 侧 insertItem(..., true) simulate 求得（SlotItemHandler.java:118-134）+ min(slotLimit, itemLimit) 收敛 | putStack → handler.setStackInSlot（:89 朴素写）；insert 路径 handler 自算 limit | **是**：广告面即 simulate，与写入面同源（自洽） | 广告面 remainder 反推容量 | T3 已消除虚报面；E25 已移除（勘误） | **勘误 + 保留**（E26） |
| E01 Container.mergeItemStack | @WrapOperation / mergeItemStack / Slot.getSlotStackLimit()I（ContainerMixin.java:14-26） | resolveMergeSlotLimit = min(declaredSlotLimit, inventoryLimit>0? : 64) + 动态规则 | 三处调用点收紧（:642/:701/:703）→ slot.putStack 落库；shrink/splitStack 对保证守恒 | 无 | mergeItemStack 返回 false 表示余量存在 | 广告=写入（同一调用点值），自洽；WrapOperation 已链式 | 保留 |
| E06 掉落 spawnItemStack | @Inject HEAD cancellable 完整接管（InventoryHelperMixin.java:17-53；vanilla InventoryHelper.java:37-72） | stack.getMaxStackSize() 派生分块边界 | 循环 splitStack(chunk) → spawnEntity 直至空 | 无 | **remainder 恒 0**（splitStack 递减循环不变量） | **高风险**：HEAD 接管无共存；分块分布与原版不同（预期行为）；**无行为测试** | 保留；测试门先行 |
| E02/E03 命令补给 | @ModifyExpressionValue ×2（CommandGiveMixin.java:11-20、CommandReplaceItemMixin.java:11-20，已链式） | getCompatibilityStackSize() | CommandGive：parseInt 上限 → addItemStackToInventory → addResource 夹取（自洽）；CommandReplaceItem：setInventorySlotContents/entity.replaceItemInInventory——朴素 setter 无夹取 | 无 | 无 | t2a §2.5 已标：朴素 setter 目标若上限未被同源扩展则断链 | 保留（已链式）；朴素目标待 T3/T13.2 闭合 |
| E20-E22 掉落渲染 | @ModifyReturnValue getModelCount + @ModifyConstant ×2（RenderEntityItemMixin.java:15-37） | 展示面对数分档 | 无（纯渲染） | 无 | 无 | 无容量风险 | 保留 |
| E14/E15 客户端槽同步 | @WrapOperation ×2（NetHandlerPlayClientMixin.java:17-51，已链式） | 无广告 | putStackInSlot/setAll 写入后条件恢复（ClientSlotSyncHooks.kt:8-35） | 无 | 无（客户端显示面） | 客户端恢复值可能超过槽位广告上限（显示层不一致，无服务端影响） | 保留 |
## 2. AE2/RS 投喂路径
| 路径 | 注入形态 | 广告面 | 写入面 | simulate | remainder | 风险 | 迁移顺序 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| L01 AE2 addItems | **已无注入**（方案 A，2026-08-08）：`AppEngAdaptorItemHandlerMixin.java` 全文 20 行，仅 `@Pseudo` + `@Mixin(targets = "appeng.util.inv.AdaptorItemHandler", remap = false)`，无 handler、无 require、无 Q-DESC-08/Q-REQ0-02 | 无独立广告 | `AdaptorItemHandler#addItems` 对 `IItemHandler#insertItem` 调用**原样执行**（热路径零分支零分配），我方不干预 | 不适用（我方不观察） | 不适用（由原版/Forge remainder 契约结构性保证，AE2 侧自行回收） | **已降级**：原「未知 handler 分块非原子 + operation 链跳过」随注入移除消失；AE2 写入面容量的无源码不可判定属性不变 | **无需迁移**（注入已移除） |
| L16/L17 RS onExtract | @WrapOperation / Math.min(JJ)J（ItemGridHandlerMixin.java:13-24、Portable 同，descriptor 已闭合） | 提取量上界 = expandDefaultExtractLimit(requested, maxItemSize) | RS 内部取货入玩家库存（无源码） | 无 | 提取侧无 remainder | 哨兵语义：requested==64 精确请求被扩为真实上限（RefinedStorageExtractHooksTest 已固化） | 保留（已闭合） |
| L18 RS StorageMonitor.extract | @ModifyConstant / extract / 64（NetworkNodeStorageMonitorMixin.java:12-15，require=0，Q-DESC-19 未闭合） | 64 → getCompatibilityStackSize() | 同 L16 | 无 | 无 | Q-DESC-19 未闭合 → 不进迁移清单 | 保留 |
## 3. T12 守恒审计接入点（**已归档**：ConservationAuditor/ConservationEvent 已从工作副本移除）
- 审计器：`ConservationAuditor.kt`、`ConservationEvent.kt` **当前不存在于 `src/`**（全仓 `grep -rl Conservation src/` 仅命中 `Ae2ItemHandlerInsertLimiter.java` 的注释与两个测试文件）；方案 A 后审计器随 `insertWithAudit` 一并移除，本节为历史方案记录。
- 历史接入点 1（L01 真实投喂）：`Ae2ItemHandlerInsertLimiter.insertWithAudit`（**已不存在**；现为 `insertCapped`，仅测试/探针直接调用，不产生守恒事件）；历史接入点 2（开发自动化）：`DevAutomationServerDriver.probeTarget`（当前 :254-288）。
- 不变量仍然成立：全仓无真实写入后回填/重试/补偿；无 remainder 重算。守恒公式现由 `VanillaInventoryConservationTest`、`Ae2ItemHandlerInsertLimiterTest` 等行为护栏承担。
## 4. UNKNOWN 条目（不进迁移清单）
1. E12/E13（OBS-06）：ItemStackNbtMixin 三个 shadow 无 refmap 条目。
2. ~~L01（Q-DESC-08）：AE2 AdaptorItemHandler descriptor 未闭合 + 无源码。~~ **已作废**：注入已移除，descriptor 问题不再存在。
3. L18（Q-DESC-19）：RS StorageMonitor descriptor 未闭合。
4. L06（Q-DESC-13）：AppEngPatternTermMixin 反射实现（违反反射禁令），BLOCKED。
5. L07：@Shadow stackLimit 字段面运行期未验证。
6. late 其余 descriptor 未闭合处当前为 13 处（原 15 处中 L01/L03/L05 已随注入移除）。
7. E16/E17/E18/E19/E06/E12/E13 运行期行为无自动化矩阵覆盖（T12.5 未闭合，T13 维持 BLOCKED）。
8. **新条目**：t14.2 E25 与工作副本不符（T3 已移除）；t14.2 计数「50→49」口径亦与当前实测（54 处）不符，见 t14.1 §7 实测口径。
## 5. 高风险项标注与迁移顺序
**高风险（保留，测试门先行）**：H1 完整重写式 HEAD 接管 6 处（E06/E13/E16/E17/E18/E19）；H2 E12 构造中途锚点；H3 E13 NBT schema 变化跨端相等性；~~H4 L01 未知 handler 分块非原子 + operation 链跳过~~（**已消失**：注入移除）；H5 E16 双端规则一致性；H6 L06 反射实现。**迁移顺序**：M1 E04 @Redirect → @ModifyExpressionValue（**已完成**，当前全仓 @Redirect 0 处）；M2 E07/E08/E09 ×3 → @ModifyExpressionValue（**已完成**，P0 事实 c 保持 2 调用方）；勘误：t14.2 E25 标记「已由 T3 移除」；测试门：E06/E12/E13/E16/E17/E18/E19 补行为测试通过前不评估形态迁移；L06 反射 → @Shadow/@Accessor（Q-DESC-13 闭合后，当前 BLOCKED）。
## 6. 状态 / 复核
- 状态：`PARTIAL`（原审记录：审计完整；E25 勘误需 t14.2 联动修订；待独立复核）。建议：T14.3 实施前由行为/字节码复核代理对 H1 组做控制流复核，并对 E25 勘误三方确认。
