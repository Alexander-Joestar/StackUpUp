# T14.5 配置与失败诊断收敛——审计部分（只审计，不实施）

> 任务：docs/agent/重构任务清单.md T14.5「配置与失败诊断收敛」的**审计部分**。
> 性质：只读取证 + 本文档产出；**未修改任何 JSON/Mixin/loader/配置/日志断言**。
> 租约：仅写本文档（docs/agent/t14.5-配置诊断审计.md，新建）；其余路径一律未写。
> 证据基线：2026-08-08 工作副本；src/main/resources 13 个 mixins.*.json；src/main/kotlin 的 StackUpUpCore.kt / StackUpUpLateMixinLoader.kt / config/MixinToggles.kt / Constants.kt；run/logs/latest.log（CLIENT 2026-08-08 运行日志）与 run/logs/2026-08-08-1.log.gz（SERVER）；mixinbooter-10.7.jar（gradle 缓存 3fdfffe4fd446461ea9c3cb044484588997b9dbc）字节码反汇编（javap）。
> 交叉引用：配置/注入账本以 t14.1-mixin-inventory.md（Q-REQ0 22 处）与 t14.2-注入语义分类.md 为准；provider/初始化/refmap 以 t14.4-extras-provider审计.md 为准；容量站点以 t2a-容量站点登记表.md 为准；高风险路径以 t14.3-高风险路径审计.md（H1 组无行为测试）为准。
> 代码路径一律 repo-relative；行号与工作副本一致。
>
> **实施收敛见 §11（2026-08-08 实施部分）**：本文件 §0~§10 为审计当时的证据与结论（四项停止条件全部命中）；
> §11 记录实施后的停止条件逐项状态、实际变更与验证结果，审计结论中与 §11 冲突的表述以 §11 为准。

## 0. 结论摘要

1. **配置分层现状**：13 个 JSON（early 1 + late 12）与两个 loader 双向 13/13 对齐，无未注册项（t14.1 §1/§5.1 一致，Q-UNREG 无）。early = core 层（required 缺省 true，20 注册项）；late 12 个 = optional 层（6 个 `required:false`，6 个无 required 键 → 缺省 true，见 §1 表）。
2. **require 现状**：显式 `require=0` 共 **22 处**（Q-REQ0-01~22，与 t14.1 §5.3 逐条对应，源码行号已核，§6.1）；全仓无 `@Expect`；无显式 `require>0`。
3. **关键新证据（字节码）**：mixinbooter-10.7.jar 内嵌 Mixin 核心（0.8.7 fork）中，**注入注解缺省 `require` 的 requiredCallbackCount = 0**（`MixinConfig.getDefaultRequiredInjections()` 恒读 `injectorOptions.defaultRequireValue`，构造为 0，与配置级 `required` 无关）。因此 **early 核心 27 处无显式 require 的注入点同样默认静默**——「核心注入 fail-fast」现状**不成立**；目标类缺失（非 pseudo）仅 WARN 不崩溃（除非 strict）；崩溃路径仅限 minVersion/feature 不满足（required 配置）、显式 require>0 注入点缺失、target 加载过早。
4. **optional 缺失日志现状**：mod 缺失/toggle 关闭 → loader 跳过**零日志**（LoadControllerMixin 字节码确认）；注入点缺失（require=0/缺省）→ **零日志**；@Pseudo 目标缺失 → DEBUG 级（默认不可见）。「optional 缺失无日志即成功」现状成立，**命中 T14.5 停止条件**。
5. **配置未注册检测**：当前无任何自动检测机制；`StackUpUpLateMixinLoader.kt:12` 的 `?: return true` 是「未知配置无条件入队」的结构面（当前无违规项，未来新增 JSON 忘登记时会静默全量装载）。
6. **勘误（相对 t14.1 OBS-02）**：「SimpleInventoryMixin 无 require → 默认 require=1、Q-DUP-01 硬失败面」在本运行时（10.7 内嵌 0.8.7）**不成立**——缺省 requiredCallbackCount=0，Q-DUP-01/02 双配置上下文均静默。风险面从「硬失败」修正为「双 JSON 装载 + 静默失效」（UNKNOWN 面不变，§6.2）。

## 1. core/optional 分层表（配置 | 类别 | require 值 | 缺失行为现状）

| 配置（src/main/resources/） | 类别 | 配置级 required | 注入 require 现状 | 缺失行为现状（10.7 运行时） |
| --- | --- | --- | --- | --- |
| mixins.stackupup.early.json（:7-31 mixins 17 + client 3；Constants.kt:38；StackUpUpCore.kt:88-92 冲突检测通过才返回） | **core**（原版/Forge 基础路径） | 缺省 true | 19 处无显式 require（缺省 0）+ Q-REQ0-01（early/VanillaInventoryLimitMixin.java:46 require=0） | 目标类缺失：WARN+跳过（非 pseudo 不崩溃）；注入点缺失：**静默**（缺省 0 与显式 0 相同）；minVersion 0.8 满足（early.json:5-6）不触发版本崩溃路径 |
| mixins.stackupup.late.ae2.json（4 注册项） | optional | 缺省 true | Q-REQ0-02~07 require=0 | loader 级：mod 缺失/toggle off → **零日志跳过**；@Pseudo 目标缺失 → DEBUG 静默；注入点缺失 → 静默 |
| mixins.stackupup.late.actuallyadditions.json（1） | optional | 缺省 true | Q-REQ0-20 require=0 | 同 ae2 |
| mixins.stackupup.late.brandonscore.json（1；**缺 minVersion、缺 compatibilityLevel**，:2 required:false） | optional | **false** | Q-REQ0-08/09 require=0 | 同 ae2；缺 minVersion 时 MixinConfig 会 WARN（§4 矩阵） |
| mixins.stackupup.late.cyclopscore.json（1；**缺 compatibilityLevel**，:3 required:false） | optional | **false** | 无显式 require（**缺省 0**）→ Q-REQ0 表外静默注入点（SimpleInventoryMixin） | 同 ae2；缺 compatLevel 同上 WARN |
| mixins.stackupup.late.enderio.json（2，:2 required:false） | optional | **false** | Q-REQ0-11/12 require=0 | 同 ae2 |
| mixins.stackupup.late.ic2.json（1） | optional | 缺省 true | Q-REQ0-16 require=0 | 同 ae2 |
| mixins.stackupup.late.immersiveengineering.json（2，:3 required:false） | optional | **false** | Q-REQ0-13/14/15 require=0 | 同 ae2 |
| mixins.stackupup.late.integrateddynamics.json（1，:3 required:false） | optional | **false** | Q-REQ0-22 require=0 | 同 ae2 |
| mixins.stackupup.late.limelib.json（1，:3 required:false） | optional | **false** | Q-REQ0-10 require=0 | 同 ae2 |
| mixins.stackupup.late.mantle.json（1） | optional | 缺省 true | Q-REQ0-21 require=0 | 同 ae2 |
| mixins.stackupup.late.refinedstorage.json（3） | optional | 缺省 true | Q-REQ0-17/18/19 require=0 | 同 ae2 |
| mixins.stackupup.late.storagenetwork.json（2；**Q-DUP-01/02 重复注册** SimpleInventoryMixin、TileInventoryMixin） | optional | 缺省 true | 复用类的注入注解同 cyclopscore.json/mantle.json 上下文 | 双配置同 mixin 装载行为未运行验证（UNKNOWN，t14.1 §8-3）；Q-DUP-01 在 storagenetwork 上下文注入缺省 0 → 静默（勘误 §0-6） |

loader 注册面：early 由 `StackUpUpCore.kt:88-92`（`getMixinConfigs` 仅在 `ensureConflictState().isEmpty()` 时返回 EARLY_MIXIN_CONFIG，否则空表 → **early 不装载且无 mixin 级日志**，仅业务层 StackUpUp.kt:62-72/98/108/116 经 ProxyClient.markConflictDisabled(:95) 客户端提示，非机器报告）；late 12 个由 `StackUpUpLateMixinLoader.kt:18-31` 注册，装载条件 `:13-14`（`isModPresent(module.modId)` && `MixinToggles.*`）。`MixinToggles.kt:3-26` 12 项全部默认 true，**仅 late loader 引用**（无 GUI/命令/持久化 → 实质编译时常量，toggle 关闭同样零日志）。

## 2. 失败诊断现状（任务第 2 项）

### 2.1 核心注入是否 fail-fast —— **现状：否**（命中 T14.5 停止条件）

字节码证据（mixinbooter-10.7.jar 内嵌 org/spongepowered/asm/*，2025-01-08 构建，运行日志确认运行时唯一 Mixin 核心）：

- `InjectionInfo.<init>`：`requiredCallbackCount=0`（putfield offset 75-76）、`expectedCallbackCount=1`（offset 70-71）、`maxCallbackCount=Integer.MAX_VALUE`（offset 79-82）。
- `InjectionInfo.parseRequirements`：注解无 `require`（或 ≤ -1）且 group 为 default → `requiredCallbackCount = MixinTargetContext.getDefaultRequiredInjections()` → `MixinConfig.getDefaultRequiredInjections()` → **`injectorOptions.defaultRequireValue` = 0**（`MixinConfig$InjectorOptions.<init>` iconst_0 putfield；MixinConfig 中无任何 putfield defaultRequireValue 的其它写点，也无 "injectors"/"defaultRequire" JSON 键字符串）→ **配置级 `required=true` 不抬高注入默认值**。
- `InjectionInfo.validate`：`injectedCallbackCount < requiredCallbackCount`（0<0 恒假）→ 缺失注入点**零日志、零异常**；仅 `require>0` 时抛 `InjectionError("Critical injection failure: ...")`。
- `MixinInfo.handleTargetError`：仅 `strict` 字段（mixin.debug.strict / config strict）→ error+throw InvalidMixinException；非 strict：soft=false → WARN 级 `@Mixin target %s was not found %s`；soft=true 且非 verbose → DEBUG 级（默认不可见）。
- `MixinInfo.readTargetClasses`：目标类缺失 → `isVirtual()`（@Pseudo）→ DEBUG `Skipping virtual target {} for {}`；非 virtual → handleTargetError(soft=false) → WARN，**不崩溃**。
- `MixinConfig` 崩溃路径（required 配置）：minVersion/requiredFeatures 不满足 → WARN + `MixinInitialisationError("Required mixin config ... requires ...")`（字节码确认；early.json:5-6 minVersion 0.8 满足，当前不触发）。

推论：early 核心 27 处无显式 require 的注入点（t14.2 E01~E28 减 Q-REQ0-01）与 Q-REQ0-01 一样在注入点层面**静默失效**；核心「fail-fast」仅剩三条窄路径（版本不满足 / require>0 / target 过早加载），均未在本项目配置中启用。**这直接违反 T14.5 禁止项「不得用 require=0、required=false 或 @Pseudo 隐藏核心目标缺失」的意图面**（early 未显式声明 require=1）。

### 2.2 optional 缺失是否有日志 —— **现状：无日志即成功**（命中停止条件）

- loader 级跳过（mod 缺失 / toggle=false）：`zone/rong/mixinbooter/mixin/LoadControllerMixin.class` 字节码——`shouldMixinConfigQueue` 返回 false 时**不产生任何日志**；仅 true 时 INFO `Adding [%s] mixin configuration.`。运行日志实证：run/logs/latest.log:59-60（early loader + Adding early）、:1054（`Loading late loader [io.alexjoest.stackupup.bootstrap.StackUpUpLateMixinLoader] for its mixins.`）——**12 个 late 配置全部跳过且零日志**（无第三方 mod 环境）；SERVER 日志（run/logs/2026-08-08-1.log.gz）同构。
- 注入点缺失（require=0 或缺省 0）：零日志（§2.1）。
- @Pseudo 目标缺失：DEBUG 级（默认不可见）。
- 全仓无 mixin 配置/目标状态的机器报告设施（rg 无 machineReport/missingTarget/configReport 命中）；ConservationAuditor（T12.3）只覆盖守恒事件，DevAutomation 探针覆盖部分兼容失败但不覆盖 mixin 装载状态。

### 2.3 配置未注册是否有检测 —— **现状：无检测**

- 当前无违规（JSON↔loader 13/13 对齐，t14.1 §5.1 Q-UNREG 无）；但无任何启动期双向校验代码。
- `StackUpUpLateMixinLoader.kt:12` `modules.firstOrNull { ... } ?: return true`：**不在 modules 表的配置会被无条件入队**（绕过 isModPresent 与 toggle 检查）——未来新增 JSON 忘登记时的静默全量装载面。
- JSON 文件缺失时的 MixinBooter/Mixin 行为未运行验证（UNKNOWN）。

## 3. 目标缺失日志/机器报告契约建议（只建议，不实施）

1. **分层声明**：early 配置 = core 层（装载必须 fail-fast）；late 12 配置 = optional 层（允许缺失但必须有日志+机器报告）。
2. **core fail-fast 门**：early 配置建议显式 `required:true` + 注入注解显式 `require=1`（或等价 strict 校验）；任何核心目标/注入点缺失 → ERROR + 启动中止（含可关闭开关，如 `-Dstackupup.core.failFast=false`，关闭态必须显式 WARN 并写入机器报告）。
3. **optional 缺失日志契约（建议字段）**：`configName`、`modId`、`targetClass`、`injectionPoint(owner/name/descriptor)`、`requireValue`、`resolution(APPLIED|SKIPPED_MOD_ABSENT|SKIPPED_TOGGLE_OFF|TARGET_MISSING|INJECTION_MISSING)`、`timestamp`、`side(CLIENT|SERVER)`。级别：TARGET_MISSING/INJECTION_MISSING 至少 WARN（当前 DEBUG/无日志）；MOD_ABSENT/TOGGLE_OFF 至少 INFO。
4. **机器报告**：启动期一次性输出（建议 `config/stackupup/mixin-report.json` 或等价路径），供 T14.7 自动化验收断言；字段与上表一致，逐配置逐注入点一行。
5. **配置未注册检测**：loader 启动期校验 `getMixinConfigs()` 返回值 ↔ JSON 资源存在性 ↔ modules 表双向对齐；不一致 → ERROR + 机器报告 +（core 层）中止。
6. **报告落点责任**：建议由 bootstrap 层（StackUpUpCore/StackUpUpLateMixinLoader 同级）统一收集，不散入各 Mixin handler；不改任何现有日志断言。
7. **与 T12.5/T14.3 关系**：H1 组（E06/E13/E16/E17/E18/E19，t14.3 §5）行为测试门先行；本契约不替代行为测试，只保证「缺失可定位」。

## 4. @Pseudo / remap=false / require 策略矩阵

| 维度 | 使用面（行号） | 10.7 运行时语义（字节码证据） | 风险/备注 |
| --- | --- | --- | --- |
| @Pseudo | late 18/18 全部（如 late/AppEngAdaptorItemHandlerMixin.java:12、late/SimpleInventoryMixin.java:10、late/TileMechanicalMachineMixin.java:9 等；early 0 处） | 目标类缺失 → `isVirtual()` → DEBUG `Skipping virtual target`（默认不可见） | 类缺失静默面；**不得用 @Pseudo 覆盖 core 目标**（当前 core 层无 @Pseudo ✓） |
| remap=false（@Mixin 级） | early 1 处（early/SlotItemHandlerMixin.java:26）+ late 18/18（全部 @Mixin(targets=..., remap=false)） | 无 refmap 条目（t14.1 §7 预期一致）；目标名以字符串直书 | late 全部依赖字符串目标名 → 类重命名/混淆变化时静默失效（配合 @Pseudo） |
| remap=false（handler/@Shadow 级） | early 3 处 handler（early/ForgeItemHandlerLimitMixin.java:38、early/ItemMixin.java:14、early/PacketUtilMixin.java:20）+ 1 处 @Shadow（early/ItemStackNbtMixin.java:35）；late 1 处 @Shadow（late/BrandonsCoreInventoryLimitMixin.java:20） | 同 @Mixin 级；OBS-06 三个 shadow 无 refmap 条目待运行验证 | ItemStackNbtMixin 为 core 路径 → 静默失效面（UNKNOWN） |
| require=0 显式 | 22 处（Q-REQ0-01~22，§6.1 表） | 注入点缺失零日志 | 全部 late + early 的 VanillaInventoryLimitMixin；合规面=必须补报告（§3） |
| require 缺省 | early 27 处 + late 1 处（late/SimpleInventoryMixin.java:16-19） | **本 fork 缺省 requiredCallbackCount=0**（非经典 1） | **核心不 fail-fast 的根因**；勘误 t14.1 OBS-02 |
| require>0 显式 | 0 处 | InjectionError「Critical injection failure」崩溃 | 建议 core 层注入点显式采用（§3-2） |
| @Expect | 0 处 | — | — |
| 配置级 required=false | 6 个 late JSON（brandonscore:2、cyclopscore:3、enderio:2、immersiveengineering:3、integrateddynamics:3、limelib:3） | 仅影响配置级错误处理（版本/feature 不满足时的崩溃门），**不影响注入缺省值**（§2.1） | 与 t14.1 §1 一致 |
| 缺 minVersion/compatLevel | brandonscore（缺两者）、cyclopscore（缺 compatLevel） | MixinConfig 对缺 minVersion 配置 WARN `Mixin config {} does not specify "minVersion"...`（字节码字符串） | OBS-03 沿用；有 WARN 但无机器报告 |
| 双 JSON 同 mixin（Q-DUP-01/02） | SimpleInventoryMixin（cyclopscore.json + storagenetwork.json）、TileInventoryMixin（mantle.json + storagenetwork.json） | 同 mixin 双配置装载行为未运行验证（UNKNOWN，t14.1 §8-3） | 注入缺省 0 → 双上下文均静默（勘误后不再有硬失败面） |


## 5. 10.7 → 11.x 加载路径差异（引用 t14.4）

- 基线：`gradle/libs.versions.toml:20` mixinbooter=10.7；`build.gradle.kts:53` modUtils.enableMixins 字符串坐标；t14.4 §1 运行闭合：唯一 provider = mixinbooter-10.7.jar 内嵌（Mixin 0.8.7 + MixinExtras 0.5.0，双端运行日志）。
- 11.13 静态证据（t14.4 §2/§3）：manifest FMLCorePlugin + **MixinConfigs: mixin.mixinbooter.init.json**，无 TweakClass/FMLCorePluginContainsFMLMod；`META-INF/services/IMixinService` → `zone.rong.mixinbooter.service.MixinBooterService`（CleanMix 平台服务）；MixinExtras 初始化入口由 `MixinBooterPlugin.initialize()` 移至 **`MixinBooterMixinPlugin`（config plugin，target 由 @env(DEFAULT) 改 @env(PREINIT)）**；POM 依赖 cleanmix 0.7.1 + mixinextras-common 0.5.5。
- 对本项目 loader 的影响面（引用 t14.4，全部 UNKNOWN 未运行验证）：①`IEarlyMixinLoader`/`ILateMixinLoader`/`Context.isModPresent` API 在 11.x 的可用性与语义（AGENTS.md 明示「若上游 11.x 已改变或淡化 early/late 机制，不得把旧规则无条件套用」）；②11.x 下注入缺省 require 语义（CleanMix 0.7.1 的 defaultRequire 规则可能不同于 10.7 内嵌 fork，需 11.x 源码+运行日志闭合）；③本项目 13 个 JSON 的 `target:"@env(DEFAULT)"` 与 11.x init.json `@env(PREINIT)` 的关系；④refmap 生成路径（CleanMix 自带 AP）无运行证据（t14.4 §4）；⑤双 ASM/CoremodClassFilter 对 com/llamalad7 类的覆盖（t14.4 §6 沿用 8.7 ④⑥）。
- 结论：10.7→11.x 全部运行行为维持 **UNKNOWN**（t14.4 UNKNOWN-A/B/C）；本审计的「缺省 require=0」「loader 跳过零日志」结论仅对 10.7 成立，迁移后必须按 11.x 实际行为重建（T14.7 把关，本审计不实施迁移）。

## 6. 问题清单

### 6.1 Q-REQ0（22 处，与 t14.1 §5.3 逐条对应，源码行号已核）

| ID | Mixin | handler | 注解类型 | 源码位置 |
| --- | --- | --- | --- | --- |
| Q-REQ0-01 | VanillaInventoryLimitMixin | stackupup$replaceCompatibilityLimit | ModifyReturnValue | early/VanillaInventoryLimitMixin.java:46 |
| Q-REQ0-02 | AppEngAdaptorItemHandlerMixin | stackupup$capUnknownHandlerInsert | WrapOperation | late/AppEngAdaptorItemHandlerMixin.java:21 |
| Q-REQ0-03 | AppEngInternalAEInventoryMixin | replaceCompatibilityLimit | ModifyConstant | late/AppEngInternalAEInventoryMixin.java:18 |
| Q-REQ0-04 | AppEngInternalAEInventoryMixin | stackupup$expandReturnedLimit | ModifyReturnValue | 同文件:23 |
| Q-REQ0-05 | AppEngInternalInventoryMixin | replaceCompatibilityLimit | ModifyConstant | late/AppEngInternalInventoryMixin.java:18 |
| Q-REQ0-06 | AppEngInternalInventoryMixin | stackupup$expandReturnedLimit | ModifyReturnValue | 同文件:23 |
| Q-REQ0-07 | AppEngPatternTermMixin | stackupup$expandBlankPatternSlotLimit | Inject | late/AppEngPatternTermMixin.java:24 |
| Q-REQ0-08 | BrandonsCoreInventoryLimitMixin | stackupup$expandStackLimitOnInit | Inject | late/BrandonsCoreInventoryLimitMixin.java:30 |
| Q-REQ0-09 | BrandonsCoreInventoryLimitMixin | stackupup$expandDefaultInventoryLimit | ModifyReturnValue | 同文件:35 |
| Q-REQ0-10 | CommonTileInventoryMixin | stackupup$unlimitInventorySlots | ModifyConstant | late/CommonTileInventoryMixin.java:12 |
| Q-REQ0-11 | EnderIOMachineInventoryLimitMixin | stackupup$expandDefaultMachineLimit | ModifyReturnValue | late/EnderIOMachineInventoryLimitMixin.java:22 |
| Q-REQ0-12 | EnderIOSlottedInventoryLimitMixin | stackupup$expandDefaultSlottedLimit | ModifyReturnValue | late/EnderIOSlottedInventoryLimitMixin.java:23 |
| Q-REQ0-13 | IEInventoryHandlerMixin | stackupup$expandSlotLimit | ModifyReturnValue | late/IEInventoryHandlerMixin.java:17 |
| Q-REQ0-14 | IEInventoryHandlerMixin | stackupup$expandInsertSlotLimit | ModifyExpressionValue | 同文件:28 |
| Q-REQ0-15 | IEMachineSlotLimitMixin | stackupup$expandSlotLimit | ModifyReturnValue | late/IEMachineSlotLimitMixin.java:36 |
| Q-REQ0-16 | InvSlotMixin | replaceCompatibilityLimit | ModifyConstant | late/InvSlotMixin.java:12 |
| Q-REQ0-17 | ItemGridHandlerMixin | stackupup$expandDefaultExtractLimit | WrapOperation | late/ItemGridHandlerMixin.java:16 |
| Q-REQ0-18 | ItemGridHandlerPortableMixin | stackupup$expandDefaultExtractLimit | WrapOperation | late/ItemGridHandlerPortableMixin.java:16 |
| Q-REQ0-19 | NetworkNodeStorageMonitorMixin | stackupupReplaceExtractLimit | ModifyConstant | late/NetworkNodeStorageMonitorMixin.java:12 |
| Q-REQ0-20 | TileEntityInventoryBaseMixin | replaceCompatibilityLimit | ModifyConstant | late/TileEntityInventoryBaseMixin.java:12 |
| Q-REQ0-21 | TileInventoryMixin | replaceCompatibilityLimit | ModifyConstant | late/TileInventoryMixin.java:12 |
| Q-REQ0-22 | TileMechanicalMachineMixin | stackupup$unlimitMachineSlots | ModifyConstant | late/TileMechanicalMachineMixin.java:12 |

### 6.2 本审计新增条目

- **Q-REQ0-CORR-01（勘误 t14.1 OBS-02 / §5.4 Q-DUP-01）**：「SimpleInventoryMixin 无 require 属性 → 默认 require=1、与其余 17 个 late mixin 模式不一致、Q-DUP-01 存在 require=1 硬失败面」——本运行时（mixinbooter-10.7.jar 内嵌 0.8.7 fork）注入缺省 requiredCallbackCount=**0**（§2.1 字节码证据），cyclopscore.json 与 storagenetwork.json 两个上下文下均为静默。t14.2 §4-4「L19 另有 require=1 硬失败面」同步失效。风险面修正为：双 JSON 装载未验证（UNKNOWN）+ 静默失效无报告。
- **Q-REQ0-NEW-01（默认 require=0 掩盖核心错误）**：early 27 处注入点无显式 require → 缺省 0 → 核心注入点缺失（refmap 失效、方法改名、混淆变化）**静默**。违反「核心注入 fail-fast」目标与 T14.5 禁止项「不得把默认 require=0 掩盖错误归类为兼容」。
- **Q-REQ0-NEW-02（optional 缺失无日志无报告）**：loader 跳过零日志 + 注入点缺失零日志 + @Pseudo 目标缺失 DEBUG → 「optional 缺失无日志即成功」现状成立（§2.2）。
- **Q-REQ0-NEW-03（配置未注册无检测）**：`StackUpUpLateMixinLoader.kt:12` `?: return true` 未知配置无条件入队；无启动期双向校验（§2.3）。
- **Q-REQ0-NEW-04（MixinToggles 非可配置）**：12 项默认 true 且无 GUI/命令/持久化（MixinToggles.kt:3-26；全仓引用仅 StackUpUpLateMixinLoader.kt:19-30）→ 关不掉、关时无日志。
- **Q-REQ0-NEW-05（冲突禁用静默）**：StackUpUpCore.kt:88-92 冲突时 early 配置不返回（空表），无 mixin 级日志；业务层仅客户端 chat 提示（StackUpUp.kt:62-72、ProxyClient.kt:95），无机器报告。

### 6.3 沿用条目（引用，未重做）

- Q-DUP-01/02（t14.1 §5.4）：双 JSON 重复注册；同 mixin 双配置装载行为 UNKNOWN。
- Q-DESC-08~23（t14.1 §5.2）：late 第三方签名未闭合（第三方 jar 缺失，T2a §5），注入点静默面无法运行验证。
- Q-AMB-01/02（t14.1 §5.4）：`<init>*` 通配与无 descriptor 方法注入的静默多命中/空命中面。
- OBS-01/03/04/05/06/07（t14.1 §5.5）：其中 OBS-02 按 §6.2 勘误；OBS-03 补充 MixinConfig WARN 行为（§4 矩阵）。
- L06 反射禁令（t14.2 §4-5、t14.3 §2）：AppEngPatternTermMixin 反射实现禁止保留（BLOCKED）。
- H1 组无行为测试（t14.3 §5）：E06/E13/E16/E17/E18/E19 测试门先行，不因本审计变更。

## 7. 验证

- **JSON↔loader 双向**：13 JSON ↔ StackUpUpCore.getMixinConfigs（StackUpUpCore.kt:88-92）+ StackUpUpLateMixinLoader.modules（:18-31）13/13 对齐；与 t14.1 §1/§2 一致（Q-UNREG 无）。命令：`grep -n "required" src/main/resources/mixins.stackupup.late.*.json`（6 个 required:false 行号已核，§1 表）。
- **Q-REQ0 22 处**：`rg -n "require\s*=|@Pseudo|remap\s*=\s*false" src/main/java/io/alexjoest/stackupup/mixin/` 与 t14.1 §5.3 逐条对应（§6.1 表，22/22 命中；SimpleInventoryMixin 无 require 命中 → 缺省面）。
- **运行时语义**：javap 反汇编 mixinbooter-10.7.jar（gradle 缓存 3fdfffe4fd446461ea9c3cb044484588997b9dbc）：
  - `zone.rong.mixinbooter.mixin.LoadControllerMixin`（late loader 路径：`Loading late loader [%s]...`、`Adding [%s] mixin configuration.`、`Failed to execute late loader [%s].`；shouldMixinConfigQueue=false 无日志）；
  - `zone.rong.mixinbooter.MixinBooterPlugin`（early loader 路径：`Loading early loader %s...`、`Adding [%s]...`、`Failed to execute early loader [%s].`）；
  - `org.spongepowered.asm.mixin.injection.struct.InjectionInfo`（构造缺省 requiredCallbackCount=0/expectedCallbackCount=1/maxCallbackCount=MAX_VALUE；parseRequirements 缺省走 getDefaultRequiredInjections；validate 不足时 InjectionError `Critical injection failure`）；
  - `org.spongepowered.asm.mixin.transformer.MixinConfig` / `MixinConfig$InjectorOptions`（getDefaultRequiredInjections → injectorOptions.defaultRequireValue=0；`Required mixin config` MixinInitialisationError 路径；`does not specify "minVersion"` WARN 字符串）；
  - `org.spongepowered.asm.mixin.transformer.MixinInfo`（readTargetClasses：virtual→DEBUG、非 virtual→handleTargetError(soft=false)→WARN；handleTargetError：strict→error+throw、soft&&非verbose→DEBUG、否则 WARN）。
- **运行日志**：run/logs/latest.log:59-60（early Adding）、:1054（late loader 加载后无任何 late Adding → 12 配置零日志跳过）；run/logs/2026-08-08-1.log.gz（SERVER 同构）。早期证据 logs/t8.0-runclient.out:185 同构。
- **未执行（如实记录）**：无第三方 mod 运行矩阵（12 mods jar 缺失，T2a §5）→ late 注入命中面 UNKNOWN；11.x 无运行证据（t14.4）→ 差异面 UNKNOWN；故意制造缺失注入点的负向运行验证未执行 → §2.1 字节码结论未经负向运行确认（剩余风险 1）。

## 8. 剩余风险

1. §2.1 的「缺省 require=0」为字节码推导结论，与经典 Mixin 文档行为（config required → 默认 1）不一致处**以本运行时字节码为准**；建议 T14.7 用故意缺失注入点的负向运行验证闭合（当前未执行，不得宣称已运行验证）。
2. late 18 个第三方目标（T2a §5，12 mods jar 缺失）→ Q-REQ0-02~22 与 Q-DESC-08~23 的注入点命中/静默面未运行验证（UNKNOWN，t14.1 §8 沿用）。
3. Q-DUP-01/02 双配置同 mixin 装载行为未验证（UNKNOWN）。
4. OBS-06（ItemStackNbtMixin 三个 shadow 无 refmap 条目）在 refmap 失效场景下 early 核心静默面未验证。
5. 11.x 全部运行行为 UNKNOWN（t14.4 UNKNOWN-A/B/C + ①③④⑤⑥）；迁移后本审计 §2.1/§2.2 结论需按 11.x 实际行为重建。
6. 配置未注册的自动检测、机器报告、core fail-fast 门均为契约建议（§3），未实施；实施前 T14.5 停止条件（核心缺失不 fail-fast、optional 缺失无日志、配置未注册未被发现、默认 require=0 掩盖错误）**全部命中**，收敛不得宣称通过。

## 9. AGENTS 六栏输出

1. **状态**：`PARTIAL`——审计取证与本文档完整（13 配置分层、Q-REQ0 22 处逐条、@Pseudo/remap=false 全量、10.7 运行时缺失行为字节码证据、运行日志证据），但 T14.5 目标状态为「现状不达标」：核心注入不 fail-fast、optional 缺失无日志、配置未注册无检测、默认 require=0 掩盖核心错误——四项停止条件全部命中，仅审计交付，不构成收敛通过；且字节码推导的负向运行验证未执行。
2. **实际变更**：仅新建 `docs/agent/t14.5-配置诊断审计.md`（本文件）。未修改任何 JSON、loader、Mixin、配置、日志断言、测试或其它文档。
3. **证据**：见 §7。核心证据=13 个 JSON 文件（行号 §1 表）+ StackUpUpCore.kt:68-92 / StackUpUpLateMixinLoader.kt:9-14,18-31 / MixinToggles.kt:3-26 / Constants.kt:38-50 + mixinbooter-10.7.jar 字节码（InjectionInfo/MixinConfig/InjectorOptions/MixinInfo/LoadControllerMixin/MixinBooterPlugin/IEarlyMixinLoader/ILateMixinLoader/Context）+ run/logs/latest.log:59-60,1054 与 2026-08-08-1.log.gz（SERVER）+ t14.1/t14.2/t14.3/t14.4 台账交叉引用。
4. **验证**：已执行=JSON/loader 双向核对、Q-REQ0 源码 grep 22/22、javap 反汇编 9 个类、运行日志双端核对；未执行=第三方 mod 运行矩阵、11.x 运行、负向缺失注入运行验证（均如实标注 UNKNOWN，未包装为通过）。
5. **剩余风险**：§8 六条；核心为字节码推导未经负向运行确认 + late 目标无源码不可判定 + 11.x 全 UNKNOWN。
6. **复核**：独立复核待指派；未完成独立复核前不得宣称 PASS（与 t14.1/t14.2 复核状态一致，由领导者统一调度）。

## 10. 复核记录

- 作者：T14.5 配置/日志审查代理（只读）；未修改任何生产配置/loader/Mixin/JSON。
- 独立复核：待指派。

## 11. 实施收敛记录（2026-08-08，T14.5 实施部分）

> 本小节由 T14.5 实施代理追加，记录四项停止条件的实施收敛；§0~§10 审计结论保持原样作为证据。
> 实施红线遵守情况：未改任何 Mixin 注解/JSON 语义；mixinbooter 仍锁定 10.7；未新增 @Redirect；未引入反射（@Mixin 为 CLASS retention，运行时反射不可见，注解检查用 ASM 读类字节）；`require=0`/缺省 0 全部保留，配合日志与 fail-fast 门；未建 Service/Manager，校验逻辑为单文件 object。

### 11.1 四项停止条件逐项状态

| 停止条件（审计 §0/§2） | 状态 | 证据 |
| --- | --- | --- |
| 1. 核心注入不 fail-fast（early 冲突时静默禁用 + 核心配置缺失零日志） | **已关闭** | `StackUpUpCore.kt:88-92`→`getMixinConfigs()`：冲突命中时 ERROR 日志点名配置与冲突 mod 后返回空表（冲突禁用设计语义保留）；核心 early 配置缺失/类登记无效时 `MixinConfigRegistrationValidator.requireCoreConfigValid`（`MixinConfigRegistrationValidator.kt`）逐问题 ERROR + 抛 `IllegalStateException`（fail-fast）。10.7 边界说明：MixinBooter `MixinBooterPlugin.loadEarlyLoaders` 对 `getMixinConfigs()` 的抛异常整体捕获并 ERROR `Failed to execute early loader [%s].`（sources 证据），因此 fail-fast 表现为「我方 ERROR + 堆栈 + MixinBooter ERROR」，不再静默返回空表；进程不中止是 10.7 加载器边界约束，见剩余风险 11.5-1。冲突检测失败（`detectConflictingCoremods` 反射异常）由静默 `emptyList` 改为 ERROR + 抛异常（`StackUpUpCore.kt:52-56`）。 |
| 2. optional 缺失无日志（late loader 跳过零日志；required:false 缺失不记录） | **已关闭（loader 级）** | `StackUpUpLateMixinLoader.shouldMixinConfigQueue` 三分支结构化日志：未知配置 ERROR `not registered in the module table`、mod 缺失 INFO `required mod '{}' is not present`、toggle 关闭 INFO `MixinToggles.{} is disabled`；required:false 配置（brandonscore 等 6 个）走同一路径，同样记录。日志断言测试：`StackUpUpLateMixinLoaderSkipLogTest`（log4j2 ListAppender 捕获实际 logger 输出）。注：注入点级缺失（require=0/缺省 0，Mixin 核心行为）仍零日志——归停止条件 4，不在 loader 层可改。 |
| 3. 配置未注册无检测（JSON↔loader 双向无校验；未知配置 `?: return true` 无条件入队） | **已关闭** | 新增 `MixinConfigRegistrationValidator`：正向（运行时）`validateConfig/validateConfigs` 校验「loader 注册的每个配置有 JSON 资源 + JSON 列出的每个类存在且带 @Mixin 注解」；early 走 `requireCoreConfigValid` fail-fast，late 走 `getMixinConfigs()` ERROR 记录不中止（late 抛异常会连带中止全部 late 装载，LoadControllerMixin 证据）。反向方向：`findUnregisteredConfigFiles`（JSON 存在但 loader 未注册）+ 测试源码枚举（带 @Mixin 未登记）在 `MixinConfigRegistrationAlignmentTest` 覆盖（jar 内无法枚举资源目录/类列表，反向只在 dev/test 可执行，见剩余风险 11.5-2）。`StackUpUpLateMixinLoader.kt:26-29` 未知配置由 `?: return true` 改为 ERROR + `return false`。 |
| 4. 缺省 require=0 掩盖核心错误（注入点缺失零日志） | **仍命中（红线内收敛）** | `require=0` 与缺省 0 全部保留（红线「不改 require=0，配合日志」），注入点级缺失（refmap 失效/方法改名/混淆变化）的静默面是 Mixin 核心 `InjectionInfo` 缺省 `requiredCallbackCount=0` 行为（审计 §2.1 字节码证据），loader 层无法改变；本次收敛把「核心配置/类缺失」从静默改为 fail-fast + ERROR、把「optional 跳过」从零日志改为结构化日志，但注入点级缺失仍需 T14.7 负向运行验证或显式 require 决策关闭，本任务红线不允许改注解语义。 |

### 11.2 实际变更（实施部分）

- `src/main/kotlin/io/alexjoest/stackupup/StackUpUpCore.kt`：companion 新增 `stackupup.coremod` logger；`detectConflictingCoremods` 区分非 Forge 环境（WARN + 空表）与真实反射失败（ERROR + 抛异常）；`getMixinConfigs` 冲突命中 ERROR 日志说明原因后返回空表（设计语义保留），非冲突路径调用 `requireCoreConfigValid` fail-fast。
- `src/main/kotlin/io/alexjoest/stackupup/bootstrap/StackUpUpLateMixinLoader.kt`：`getMixinConfigs` 启动期执行 `validateConfigs` 并 ERROR 记录问题、INFO 记录「late 目标为第三方类，目标存在性 UNKNOWN」；`shouldMixinConfigQueue` 三分支结构化日志，未知配置返回 false；`LateMixinModule` 增加 `toggleName` 用于日志点名开关。
- `src/main/kotlin/io/alexjoest/stackupup/bootstrap/MixinConfigRegistrationValidator.kt`（新建）：`Problem`、`validateConfig`、`validateConfigs`、`validateJson`（Gson 2.8.0 兼容 API）、`requireCoreConfigValid`、`findUnregisteredConfigFiles`、`logProblems`；@Mixin 注解检查用 ASM 读类字节（`ClassNode.visible/invisibleAnnotations`，CLASS retention）。
- 测试（新建，`src/test/kotlin/io/alexjoest/stackupup/bootstrap/`）：`MixinConfigRegistrationValidatorTest`（真实 classpath 全量无问题、缺资源 fail-fast、合成未注册场景、`findUnregisteredConfigFiles`）、`StackUpUpLateMixinLoaderSkipLogTest`（mod 缺失/required:false 缺失/toggle 关闭/未知配置的日志与返回值、冲突禁用 ERROR 日志）、`MixinConfigRegistrationAlignmentTest`（JSON↔loader 反向、源码类↔JSON 反向，结构检查）。
- `docs/agent/t14.5-配置诊断审计.md`：本小节（§11）+ 头部指向注记。§0~§10 未改写。

### 11.3 验证

- 已执行：`./gradlew test --tests "*MixinConfigRegistrationValidatorTest" --tests "*MixinConfigRegistrationAlignmentTest" --tests "*StackUpUpLateMixinLoaderSkipLogTest" --tests "*MixinBooterIntegrationTest" --tests "*EarlyMixinConfigTest"`（定向）；全量 `test`；`compileKotlin compileJava`；`spotlessCheck`；三护栏 `*CoremodHierarchyBytecodeSafetyTest`、`*EarlyMixinBytecodeSafetyTest`、`*MixinBooterIntegrationTest`。结果与日志行号以实施代理最终输出为准（未执行项如实标注）。
- 执行方式：IDE MCP 模块模型对 test scope 的 log4j-core 解析与 Gradle 不一致（IDE 构建报 `ListAppender` 不可解析，而 `./gradlew dependencies --configuration testCompileClasspath` 确认 log4j-core 2.17.1 在列），故测试经 `JAVA_HOME=/c/Users/25761/.jdks/jbr-25.0.4 ./gradlew --no-daemon` 兜底执行，IDE 构建仅用于编译错误反馈（IDE 侧 gson 解析为 2.8.0，与 Gradle 一致，校验器已按 2.8.0 API 编写）。

### 11.4 剩余风险

1. 核心 fail-fast 的抛异常被 MixinBooter 10.7 `loadEarlyLoaders` 捕获（sources 证据），表现为显式 ERROR + 堆栈而非进程中止；如需中止需上游行为或额外 preInit 门（`StackUpUp.kt` 不在本任务租约内）。
2. 反向核对（JSON 存在未注册 / 带 @Mixin 未登记）只在 dev/test 文件系统可执行；打包 jar 运行时无目录枚举，运行时仅正向校验。
3. 停止条件 4（注入点级 require=0 静默）仍命中：需要 T14.7 负向运行验证或显式 require 决策，超出本任务红线（不改注解语义）。
4. late 第三方目标存在性仍 UNKNOWN（T2a §5 无 jar/源码），已按 INFO 记录 UNKNOWN 而非假装校验。
5. 冲突检测失败改为抛异常后，`getASMTransformerClass` 路径（FML 侧）同样会抛——仅在不健康的 Forge 环境中触发，属预期 fail-fast。

### 11.5 复核

- 独立复核：待领导者指派；未完成独立复核前不得宣称 PASS。
