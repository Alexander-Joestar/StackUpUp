# T14.7 发布前矩阵与交接

> 状态：PARTIAL；发布准入门为 BLOCKED。当前构建基线已迁移到 MixinBooter 11.13 + CleanMix 0.7.1 + `IMixinConnector`，最近一次矩阵（2026-08-10）通过但此后代码改动未重跑；独立复核、生产混淆和第三方写入证据仍未闭合。
> 本文只记录状态、证据和验收边界，不把未运行、未复核或缺源码项目写成通过。

## 1. 当前状态表

| 任务 | 产物状态 | 验收状态 | 独立复核 | 剩余风险 |
| --- | --- | --- | --- | --- |
| T8.0 | 真实客户端 F3+T 判据曾出现，但由外部操作者触发；自动化注入未触发 | PARTIAL | 待指派 | 归属与可重复性 UNKNOWN；见 `t8.0-真实f3t基线记录.md` |
| T8.2 | 未完成 | BLOCKED | 未开始 | 缺少可执行输入与基线证据 |
| T12（方案 A） | 热路径透传方案已记录；JSONL 守恒审计已废弃 | PARTIAL（方案 A 已归档） | 待复核 | 历史 JSONL 不再是当前报告，也不再作为 T13 依赖 |
| T13 | 未形成可发布回扩矩阵 | BLOCKED | 未开始 | 容量证据和 T14.7 准入门未闭合 |
| T14.1–T14.6 | 各审计文档已产出，仍有 UNKNOWN/未执行项 | PARTIAL | 待指派 | 高风险 Mixin、缺失 jar、注入生效面和部分运行验证未闭合 |
| T14.7 | 本状态表与验收卡 | BLOCKED/PARTIAL | 待指派 | 不能解锁生产 Mixin 迁移或其他回扩任务 |
| T15 | build-logic 结构与验证分开记录 | PARTIAL | 待指派 | 结构完成不等于验证完成；以任务清单当前记录为准 |
| T16 | 未形成 | UNKNOWN | 未开始 | 依赖/发布对齐仍待后续任务，不在本文补实现 |

## 2. 当前构建与注册事实

- `gradle/libs.versions.toml:20-23,29-31`：当前版本为 MixinBooter `11.13`、CleanMix `0.7.1`。
- `build.gradle.kts` 的 useMixins 块（:49-66）：CleanMix 作为编译期 annotation processor 挂载，用于 obfuscation AP/refmap；不据此宣称生产混淆消费已验证。
- `src/main/kotlin/io/alexjoest/stackupup/bootstrap/StackUpUpMixinConnector.kt:12-32`（类声明 :33；`connect()` 及其决策函数见同文件后半）：普通 `class StackUpUpMixinConnector : IMixinConnector` 通过 manifest `MixinConnector` 注册，统一执行 early/late 配置校验、冲突处理、mod presence 与 toggle 决策。
- 10.7、`IEarlyMixinLoader`/`ILateMixinLoader`、旧 loader 和旧注册方式仅为历史证据；11.13 才是当前事实。旧 JSONL 守恒报告同样只保留为历史产物。

已记录的 11.13 证据包括：CleanMix AP 生成 refmap、dev 服务端发现并加载 connector、MixinExtras 0.5.5 初始化和 refmap remap。证据链见 `compatibility-decision-record.md` §8.6.1「迁移实施记录」与 §8.8「IMixinConnector 装载入口迁移」；这些证据不替代 T14.7 准入复核。

## 3. 验收卡

T14.7 只有在以下项目逐项有可追溯证据并经非作者复核后才能通过：

1. 当前 jar/POM/manifest、CleanMix AP、Extras provider 和 `IMixinConnector` 注册路径一致。
2. refmap 已生成并装入产物；生产混淆（notch）消费仍需实际验证，dev/SRG 证据不能代替。
3. 核心注入目标的 descriptor、匹配数、失败门、变换后字节码和高风险控制流行为分别闭合。
4. 服务端/客户端矩阵区分通过、失败、明确缺失和链接异常；单个目标异常不得中断后续目标。
5. 第三方 jar 或真实写入源码缺失的目标保持 **无源码不可判定**，不得凭类名、注册结果或静态配置放大容量。
6. 容量真实写入满足 `storedDelta + remainderCount == offered`；区分 `simulate`，不做写入后回填、重试或补偿。

当前矩阵最近一次运行通过（2026-08-10，`run/logs/autotest-report.txt` 最终状态 PASS、`run/logs/latest.log` 同批次），该次运行已推翻早先 `inv_wrapper_limit` / `sided_inv_wrapper_limit` 失败记录（修正见 `compatibility-decision-record.md` §3.12）；但此后代码改动未经矩阵重跑，不能据此宣称当前工作副本全矩阵通过。

## 4. 验证记录

| 项目 | 验证结论 | 证据/缺口 |
| --- | --- | --- |
| `compileJava`、CleanMix obfuscation AP、refmap 生成 | 通过记录 | CDR §8.6.1；当前 production notch 消费未实测 |
| `test` 与三项字节码护栏 | 通过记录 | CDR §8.8；独立复核仍待指派 |
| `spotlessCheck` | 本次通过（2026-09-14 实测） | 本 lane 实测 `spotlessCheck` BUILD SUCCESSFUL；CDR §8.8 记录的 `RuleFieldContextProvider.kt` 预存违规在当前工作副本中已不再命中 |
| `runServerAutoTestMatrix` | 最近一次通过（2026-08-10，`run/logs/autotest-report.txt` PASS） | 早先两个转发 wrapper 探针失败已按 `compatibility-decision-record.md` §3.12 修正探针预期；此后代码改动未重跑矩阵 |
| 真实客户端 F3+T | PARTIAL | `t8.0-真实f3t基线记录.md`：第二次重载由外部操作者触发，自动化注入未触发 |
| 第三方 mod 矩阵 | UNKNOWN | 目标 jar 缺失，真实写入路径无源码不可判定 |
| 生产混淆 refmap 消费 | UNKNOWN | 未在 notch 环境运行 |
| 高风险 Mixin 行为与完整客户端矩阵 | UNKNOWN | 不能以结构检查或服务端启动替代 |

## 5. T12 方案 A 与依赖边界

方案 A（历史决策）移除 AE2 热路径 wrapper/写入后补偿，保留原 `insertItem` 调用和 Forge remainder 契约；守恒 JSONL 报告、审计器及其关闭态门已归档。当前运行验证由真实行为测试和边界探针承担；历史 JSONL 不代表当前实现，也不阻塞 T13。AE2/第三方内部写入链缺源码时仍为 **无源码不可判定**。

T13 仍受自身容量与真实写入证据门控，且本矩阵准入未通过；“方案 A 不再作为 T13 依赖”不等于 T13 已通过。

## 6. 交接六栏

1. **状态**：`PARTIAL`；T14.7 准入门 `BLOCKED`。11.13/0.7.1/connector 当前事实已同步，最近一次矩阵为 2026-08-10 PASS，但此后改动未重跑；独立复核、生产混淆和第三方证据缺口仍在。
2. **实际变更**：本次仅更新本文档；未修改 Mixin、loader、依赖、构建配置、自动化源码或测试。
3. **证据**：当前配置与 connector 源码见 §2；迁移与 AP/refmap 证据见 `compatibility-decision-record.md` §8.6.1、§8.8，转发 wrapper 探针预期修正与矩阵通过见 §3.12；F3+T 见 `t8.0-真实f3t基线记录.md` §6.5「判据达成记录」与 §6.6「重载前后自有消息状态」。
4. **验证**：采用已有真实命令记录：编译/AP、`test`、字节码护栏和服务端启动有结果；本次 lane 实测 `spotlessCheck` 通过（2026-09-14）；最近一次矩阵（2026-08-10）通过，但此后代码改动未重跑；生产混淆、第三方 mod 全量矩阵和独立 T14.7 复核未完成。
5. **剩余风险**：此后改动未经矩阵重跑、T8.2/T13 为 BLOCKED、T16 为 UNKNOWN（尚未开始）、T14.1–T14.6 未独立复核、第三方 jar 缺失、notch refmap 未验证、高风险 Mixin 与客户端行为未闭合。
6. **复核**：作者为文档同步子代理；独立复核者待主代理指派。作者不宣称通过，非作者复核完成前不得改写为 `PASS`。
