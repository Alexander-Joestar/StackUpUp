# T14.7 发布前矩阵与交接（汇总）

> 任务：重构任务清单 T14.7「发布前矩阵与交接」的**汇总部分**（docs/agent/重构任务清单.md:575-586）。
> 性质：只读汇总 + 本文档写入；**未修改任何 JSON/Mixin/loader/配置/生产代码**（租约：仅写本文档，新建）。
> 证据基线：2026-08-08 工作副本 + 六份 T14.x 审计文档 + 决策记录 8.6/8.7 + T2a 台账 + 运行产物（run/logs）。
> 来源文档（每行矩阵均回链行号）：
> - t14.1 = docs/agent/t14.1-mixin-inventory.md（38/38 双向登记、Q 问题表、OBS）
> - t14.2 = docs/agent/t14.2-注入语义分类.md（50→49 注入点分类、M1/M2/M3 迁移清单）
> - t14.3 = docs/agent/t14.3-高风险路径审计.md（49 注入点、H1-H6、E25 勘误）
> - t14.4 = docs/agent/t14.4-extras-provider审计.md（10.7 双端日志闭合、11.x UNKNOWN-A/B/C）
> - t14.5 = docs/agent/t14.5-配置诊断审计.md（四项停止条件命中、Q-REQ0-NEW 条目、缺省 require=0 字节码证据）
> - t14.6 = docs/agent/t14.6-借鉴准入审计.md（6 否决/1 待证据/1 部分）
> - CDR 8.6/8.7 = docs/agent/compatibility-decision-record.md:481-603（T9-R3 迁移路径决策、T14.0 版本账本）
> - T2a §5 = docs/agent/t2a-容量站点登记表.md:156-161（缺失第三方 jar 台账）
> - 任务清单 = docs/agent/重构任务清单.md:575-586（T14.7 定义：目标/验证/停止条件/解锁项）
>
> 红线（任务清单:582-584）：任何核心/可选目标、provider、refmap、环境侧、混淆、写入面或守恒证据未闭合都不得标为通过；不得以未知
> jar、未执行测试或静态配置掩盖失败；T14.7 通过前不得执行 MixinBooter 11 迁移或生产注入迁移。本文档全篇不写「已使用 11」：
> 11.13 只是**计划迁移目标**，当前事实仍为 mixinbooter 10.7。

## 0. 结论摘要

1. **注入器决策矩阵（49 处，early 27 + late 22）**：迁移 4 处（M1: E04；M2: E07/E08/E09，全部 @Redirect → @ModifyExpressionValue）；
   保留 45 处（已闭合 29 处 + 未闭合 UNKNOWN 16 处）；禁止新增 @Redirect/@Overwrite 0 处；L06 注入形式保留但**反射实现禁止保留**
   （强制迁移 M3，阻塞于 Q-DESC-13）；E25 已由 T3 移除，非当前注入点（t14.3:10 勘误，计数 50→49）。
2. **目标完整性报告**：JSON↔类双向 38/38 全对齐，Q-UNREG 无（t14.1:35,437）；Q-DESC 23 处中闭合 7 处、**未闭合 16 处（Q-DESC-08~23，
   全部 late 第三方）**；Q-REQ0 显式 22 处 + 勘误 1 处 + 新增 5 处；Q-DUP 2、Q-AMB 2、OBS 7（其中 OBS-02 已勘误）。
3. **MixinBooter 版本决定**：11.13 为计划内必选迁移目标（CDR 8.6:505-515），迁移顺序 5 步、回滚边界 2 步、触发/停止条件齐全；**当前
   不实施**，T14.7 通过前不解锁（CDR 8.6:483-484,539-543）。
4. **未知 jar 台账**：12 个第三方 mod jar 全部缺失（T2a:156-161），late 全部目标与方法签名 **无源码不可判定**。
5. **验证清单**：10.7 侧双端运行日志/refmap/字节码/守恒审计已闭合（t14.1/t14.4/t14.5/t14.3）；三个字节码护栏测试（CoremodHierarchyBytecodeSafetyTest/EarlyMixinBytecodeSafetyTest/MixinBooterIntegrationTest）**已执行/通过**（协调者波次7 BUILD SUCCESSFUL；build/test-results XML 共 7 用例 0 失败，时间戳 2026-08-09 03:32 +08，见 §5.2）；第三方 mod 运行矩阵、11.x 运行、完整 test/runServerAutoTest/spotlessCheck、负向缺失注入运行验证**均未执行**（如实标注，不包装为通过）。
6. **状态判定**：T14.1-T14.6 各文档复核记录均为「待指派」（t14.1:584、t14.2:134、t14.5:176、t14.6:119），T14.7 汇总亦未完成独立复核 →
   本文档整体 **PARTIAL，不宣称 PASS**；T14.7 迁移准入门**未通过**，生产迁移与 11 迁移保持锁定。

## 1. 注入器决策矩阵（49 处注入点）

计数口径：t14.2 原计 50 处（early 28 + late 22，**勘误前 50 口径**）；t14.3:10 勘误——E25（SlotItemHandlerMixin.replaceCompatibilityLimit/
getSlotStackLimit）已由 T3 收敛在 e1db0e7 删除，**当前实际 49 处（early 27 + late 22）**。注入类型分布（t14.1:561-562，**勘误前 50
口径**；勘误后 49 口径见 t14.2 §7：@ModifyReturnValue 16→15）：
@Inject 9、@Redirect 4、@WrapOperation 7、@ModifyExpressionValue 3、@ModifyReturnValue 16、@ModifyConstant 11；@Accessor/
@Invoker/@Overwrite/@ModifyVariable/@ModifyArg/@ModifyReceiver 全仓 0 处（t14.2:119）。

### 1.1 迁移（4 处，M1/M2；全部 @Redirect → @ModifyExpressionValue）

| ID | 注入点（mixin.handler / 方法 / @At） | 现状语义 | 目标形态 | 前置条件 | 回滚/等价边界 | 证据 |
| --- | --- | --- | --- | --- | --- | --- |
| E04 | EntityItemMergeMixin.stackupup$useLargestMergeLimit / combineItems(EntityItem)Z / ItemStack.getMaxStackSize()I | 重定向合并门控，返回 Math.max(两栈 max) | @ModifyExpressionValue(int original, EntityItem other) | 无（证据已闭合；EntityItem.java:258 唯一调用点） | 门控等价：count1+count2 > max(...) 判定与合并落库不变 | t14.2:21；t14.3:21 |
| E07 | InventoryPlayerAddResourceMixin.stackupup$useMergeLimit / canMergeStacks / InventoryPlayer.getInventoryStackLimit()I | 重定向合并门控，返回 resolveInventoryClampLimit(incoming,...) | @ModifyExpressionValue(int original, ItemStack existing, ItemStack incoming) | 无；**P0 事实 c：resolveInventoryClampLimit 保持 2 个库存注入调用方** | 门控与夹取行为不变；调用方计数不变 | t14.2:24；t14.3:22 |
| E08 | InventoryPlayerAddResourceMixin.stackupup$usePickedItemLimit / addResource / ItemStack.getMaxStackSize()I（:341/:343 两处） | 重定向槽内栈取源 | @ModifyExpressionValue(int original, int slot, ItemStack source) | 无 | 取源统一语义保持；空槽/非空槽可达路径等价已核 | t14.2:25；t14.3:23 |
| E09 | InventoryPlayerAddResourceMixin.stackupup$usePickedStackLimit / addResource / InventoryPlayer.getInventoryStackLimit()I（:346/:348 两处） | 重定向落盘夹取 | @ModifyExpressionValue(int original, int slot, ItemStack source) | 无；**P0 事实 c 调用方之二** | 写入面=广告面，夹取上界即落库上界不变；addResource 返回 i-j 语义不变 | t14.2:26；t14.3:24 |

约束：所有迁移不得改变公共调用面与调用次数；不新增 @Redirect/@Overwrite（t14.2:98）。迁移前无需第三方 jar（E04/E07/E08/E09 全部为
vanilla/Forge 目标，descriptor 与调用点已闭合，t14.2 §7:120）。

### 1.2 保留——已闭合（29 处，不进迁移清单）

early 23 处（E25 除外全部保留）：

| ID | 注入点 | 现状语义 | 证据 |
| --- | --- | --- | --- |
| E01 | ContainerMixin @WrapOperation / mergeItemStack / Slot.getSlotStackLimit()I | 已链式；包裹合并写入上界 | t14.2:18；t14.3:31 |
| E02 | CommandGiveMixin @ModifyExpressionValue / execute / Item.getItemStackLimit()I | 已链式；命令补给上界 | t14.2:19；t14.3:33 |
| E03 | CommandReplaceItemMixin @ModifyExpressionValue / execute / 同 E02 | 已链式；朴素 setter 目标断链风险维持 t2a §2.5 | t14.2:20；t14.3:33 |
| E05 | ForgeItemHandlerLimitMixin @ModifyReturnValue / getSlotLimit（6 Forge 类，remap=false） | 改返回值（original==64 → compat） | t14.2:22 |
| E06 | InventoryHelperMixin @Inject HEAD cancellable / spawnItemStack | HEAD 完整接管掉落拆分（**H1 高风险，测试门先行**） | t14.2:23；t14.3:32 |
| E10 | ItemMixin @ModifyReturnValue / getItemStackLimit(ItemStack)I | 物品层上限单一来源 | t14.2:27 |
| E11 | ItemStackMixin @ModifyReturnValue / getMaxStackSize()I | 改返回值 | t14.2:28 |
| E12 | ItemStackNbtMixin @Inject / <init>(NBTTagCompound)V / FIELD itemDamage PUTFIELD AFTER | 构造中途锚点（**H2 高风险；OBS-06 运行期 UNKNOWN**） | t14.2:29；t14.3:26 |
| E13 | ItemStackNbtMixin @Inject HEAD cancellable / writeToNBT | HEAD 完整接管；TAG_Byte→TAG_Int schema 变化（**H3 高风险，测试门先行**） | t14.2:30；t14.3:27 |
| E14 | NetHandlerPlayClientMixin @WrapOperation / handleSetSlot / putStackInSlot | 已链式；写入后恢复 count | t14.2:31；t14.3:35 |
| E15 | NetHandlerPlayClientMixin @WrapOperation / handleWindowItems / setAll | 已链式；同 E14 | t14.2:32；t14.3:35 |
| E16 | NetHandlerPlayServerMixin @Inject HEAD cancellable / processCreativeInventoryAction | HEAD 完整接管创造校验（**H5 高风险，测试门先行**） | t14.2:33；t14.3:25 |
| E17 | PacketBufferMixin @Inject HEAD cancellable / readItemStack | HEAD 完整接管变长解码（**H1，测试门先行**） | t14.2:34；t14.3:28 |
| E18 | PacketBufferMixin @Inject HEAD cancellable / writeItemStack | HEAD 完整接管编码（**H1，测试门先行**） | t14.2:35；t14.3:28 |
| E19 | PacketUtilMixin @Inject HEAD cancellable / writeItemStackFromClientToServer | HEAD 完整接管 C→S 编码（**H1，测试门先行**） | t14.2:36；t14.3:29 |
| E20 | RenderEntityItemMixin @ModifyReturnValue / getModelCount | 展示面对数分档 | t14.2:37；t14.3:34 |
| E21 | RenderEntityItemMixin @ModifyConstant / doRender / floatValue=-0.09375F | 渲染距离常量 | t14.2:38 |
| E22 | RenderEntityItemMixin @ModifyConstant / doRender / floatValue=0.09375F | 渲染距离常量 | t14.2:39 |
| E23 | RenderItemMixin @WrapOperation / renderItemOverlayIntoGUI / drawStringWithShadow | 已链式；数量文本绘制 | t14.2:40 |
| E24 | ServerRecipeBookHelperMixin @ModifyConstant / func_194324_a / intValue=64 | 合成台转移量常量（OBS-07 srg 名直书） | t14.2:41 |
| E26 | SlotItemHandlerMixin @ModifyReturnValue / getItemStackLimit | 广告=simulate 与写入面同源（自洽）；E25 已移除后唯一遗留注入 | t14.2:43；t14.3:30 |
| E27 | SlotLimitMixin @ModifyReturnValue / getItemStackLimit(ItemStack)I | min(dynamic, inv limit)；invLimit<=0 未保证（既有限制） | t14.2:44 |
| E28 | VanillaInventoryLimitMixin @ModifyReturnValue / getInventoryStackLimit()I（13 目标，require=0） | 改返回值；original==64 哨兵待替换（T2a §6.7）；缺省 require=0 静默面见 §2.3 | t14.2:45；t14.5:23 |

late 已闭合 6 处（descriptor/refmap 证据完整）：

| ID | 注入点 | 现状语义 | 证据 |
| --- | --- | --- | --- |
| L07 | BrandonsCoreInventoryLimitMixin @Inject / <init> / RETURN | 构造 RETURN 后写 @Shadow stackLimit；**字段面运行期 UNKNOWN** | t14.2:57；t14.3:58 |
| L09 | CommonTileInventoryMixin @ModifyConstant / <init>(I)V / 64 | 构造器常量；descriptor 完整 | t14.2:59 |
| L10 | EnderIOMachineInventoryLimitMixin @ModifyReturnValue / getInventoryStackLimit()I（2 目标） | 改返回值；descriptor 完整 | t14.2:60 |
| L11 | EnderIOSlottedInventoryLimitMixin @ModifyReturnValue / getInventoryStackLimit(I)I（3 目标） | 带槽重载返回值；descriptor 完整且区分重载 | t14.2:61 |
| L16 | ItemGridHandlerMixin @WrapOperation / onExtract / Math.min(JJ)J | 已链式；提取量上界（哨兵语义由 RefinedStorageExtractHooksTest 固化） | t14.2:66；t14.3:42 |
| L17 | ItemGridHandlerPortableMixin 同 L16 | 同 L16 | t14.2:67；t14.3:42 |

### 1.3 保留——未闭合 UNKNOWN（16 处，**不进迁移清单**）

全部为 late @Pseudo + remap=false + require=0 注入点；对应 mod jar 缺失（§4），descriptor 未闭合（Q-DESC-08~23），
**无源码不可判定**，禁止按类名/方法名补 descriptor（t14.1:11,569）。同一注入点上的 Q-REQ0 编号见 §2.3。

| ID | 注入点 | 现状语义 | 未闭合点 | 证据 |
| --- | --- | --- | --- | --- |
| L01 | AppEngAdaptorItemHandlerMixin @WrapOperation / addItems / IItemHandler.insertItem | 包裹不调用 original，委托 insertCapped（限流站点，simulate/真实写入正确分离；H4 高风险：未知 handler 分块非原子） | Q-DESC-08（addItems 重载可能） | t14.2:51；t14.3:41 |
| L02 | AppEngInternalAEInventoryMixin @ModifyConstant / <init>* / 64 | 构造期常量 | Q-DESC-09（构造器通配） | t14.2:52 |
| L03 | AppEngInternalAEInventoryMixin @ModifyReturnValue / getInventoryStackLimit | 改返回值 | Q-DESC-10 | t14.2:53 |
| L04 | AppEngInternalInventoryMixin @ModifyConstant / <init>* / 64 | 构造期常量 | Q-DESC-11 | t14.2:54 |
| L05 | AppEngInternalInventoryMixin @ModifyReturnValue / getInventoryStackLimit | 改返回值 | Q-DESC-12 | t14.2:55 |
| L06 | AppEngPatternTermMixin @Inject / <init>* / RETURN | 构造 RETURN 后**反射** setStackLimit（违反 AGENTS 反射禁令；OBS-05） | Q-DESC-13；**注入形式保留、反射实现禁止（M3，BLOCKED）** | t14.2:56；t14.3:57 |
| L08 | BrandonsCoreInventoryLimitMixin @ModifyReturnValue / getInventoryStackLimit | 改返回值 | Q-DESC-14 | t14.2:58 |
| L12 | IEInventoryHandlerMixin @ModifyReturnValue / getSlotLimit | 改返回值 | Q-DESC-15 | t14.2:62 |
| L13 | IEInventoryHandlerMixin @ModifyExpressionValue / insertItem / IIEInventory.getSlotLimit(I)I | 改表达式值 | Q-DESC-16（insertItem 重载可能） | t14.2:63 |
| L14 | IEMachineSlotLimitMixin @ModifyReturnValue / getSlotLimit（16 目标） | 改返回值 | Q-DESC-17（16 目标未核实） | t14.2:64 |
| L15 | InvSlotMixin @ModifyConstant / <init>* / 64 | 构造期常量 | Q-DESC-18；Q-AMB-01 通配 | t14.2:65 |
| L18 | NetworkNodeStorageMonitorMixin @ModifyConstant / extract / 64 | 提取侧限流常量 | Q-DESC-19 | t14.2:68；t14.3:43 |
| L19 | SimpleInventoryMixin @ModifyReturnValue / getInventoryStackLimit（cyclopscore.json + storagenetwork.json，Q-DUP-01） | 改返回值；**缺省 require=0（勘误后无硬失败面，双 JSON 装载 UNKNOWN）** | Q-DESC-20 | t14.2:69；t14.5:17,90 |
| L20 | TileEntityInventoryBaseMixin @ModifyConstant / getMaxStackSize / 64 | 改常量 | Q-DESC-21 | t14.2:70 |
| L21 | TileInventoryMixin @ModifyConstant / <init>* / 64（mantle.json + storagenetwork.json，Q-DUP-02） | 构造期常量 | Q-DESC-22；Q-DUP-02 | t14.2:71 |
| L22 | TileMechanicalMachineMixin @ModifyConstant / <init> / 64 | 构造期常量 | Q-DESC-23 | t14.2:72 |

### 1.4 禁止

- **禁止新增 @Redirect / @Overwrite**：全仓 0 处现状（t14.2:119），AGENTS.md「不得新增 @Redirect」约束延续（t14.2:6）。
- **L06 反射实现禁止保留**：AppEngPatternTermMixin 反射读写第三方字段/方法违反 AGENTS 反射禁令（t14.1:530-531 OBS-05、t14.2:56、
  t14.3:57）；迁移 M3（@Shadow/@Accessor）阻塞于 Q-DESC-13 + 第三方 jar 缺失。
- **借鉴否决项（永久禁止，t14.6:114）**：S1 全局 Item、S2 常量命中即容量、S3 整方法 splice、S4 无 remainder、B2 scaleSlotLimit 与
  客户端本地 fallback、B3 丢返回值、B4 快照外独立 override（t14.6 §1:16-28 裁决表、§6:95-108 守恒风险逐项）。

### 1.5 已移除（非当前注入点）

- **E25**（SlotItemHandlerMixin.replaceCompatibilityLimit/getSlotStackLimit）：T3 收敛在 e1db0e7 删除（t14.3:10）；t14.2 表格
  遗留条目，计数按 t14.3 勘误 50→49。

### 1.6 不可共存项与回滚边界（t14.2:85-98）

- 不可共存：6 处 HEAD cancellable @Inject（E06/E13/E16/E17/E18/E19）整方法接管；4 处 @Redirect（E04/E07/E08/E09，迁移后解除）；
  L01 operation 链跳过；Q-DUP-01/02 双 JSON 装载（L19/L21）；L06 反射路径。
- 回滚边界：E04 门控等价；E07/E09 resolveInventoryClampLimit 调用方保持 2 个（P0 事实 c）；E08 取源统一语义保持；所有迁移不得改变
  公共调用面与调用次数（t14.2:94-98）。

## 2. 目标完整性报告（Q 问题表闭合状态）

### 2.1 双向登记与 Q-UNREG —— 闭合

- JSON↔类双向 38/38 全对齐（early 20 + late 18），无「JSON 注册但无 @Mixin」或「@Mixin 类但不在 JSON」项（t14.1:35-81,437-438）。
- 编译产物对照：build/classes/java/main 下 early 21 个 .class（20 个 @Mixin 类 + 非 @Mixin 辅助类 VanillaInventoryTargets，
  口径注记见 t14.1:80-81）、late 18 个 .class，均与 Java 文件一一对应（t14.1:80-81）。
- src 13 个 mixins.*.json 与 build/resources/main 同名产物逐字节一致（t14.1:5-7）；refmap 双产物 md5 相同
  （9b8225cc…，t14.4:52）；运行日志 `Remapping refMap mixins.StackUpUp.refmap.json` + `Preparing mixins.stackupup.early.json
  (21)`（双端 debug 日志实测为 "(21)"；20 注册 vs 21 prepared 为观察项）实证 refmap 运行期真实使用（t14.4:54）。

### 2.2 Q-DESC（23 处 / 19 个 mixin，t14.1:440-466）

| 状态 | 条目 | 说明 |
| --- | --- | --- |
| **闭合**（7 处） | Q-DESC-01/02/04/05 | refmap/AP 已解析，无歧义（t14.1:444-448） |
| **闭合**（3 处，低歧义） | Q-DESC-03/06/07 | remap=false 无 refmap；由接口唯一方法 / @Shadow 佐证 / T2a §2.2-2.3 判定签名（t14.1:446,449-450） |
| **未闭合 UNKNOWN**（16 处） | Q-DESC-08~23 | 全部 late 第三方目标，mod jar 缺失（§4），**无源码不可判定**；按 T14.1 禁止项标记 UNKNOWN，不以类名/方法名猜 descriptor（t14.1:451-466,569） |

descriptor 完整（不在 Q-DESC 表）：ContainerMixin、EntityItemMergeMixin、InventoryHelperMixin、InventoryPlayerAddResourceMixin×3、
ItemMixin、ItemStackMixin、ItemStackNbtMixin.<init>、NetHandlerPlayClientMixin×2、NetHandlerPlayServerMixin、PacketBufferMixin×2、
PacketUtilMixin、RenderEntityItemMixin×3、RenderItemMixin、SlotLimitMixin、VanillaInventoryLimitMixin、CommonTileInventoryMixin.
<init>(I)V、EnderIOMachineInventoryLimitMixin.getInventoryStackLimit()I、EnderIOSlottedInventoryLimitMixin.getInventoryStackLimit(I)I、
ItemGridHandlerMixin×2.onExtract（t14.1:468-472）。

### 2.3 Q-REQ0（显式 22 处 + 勘误 1 + 新增 5，t14.1:474-499、t14.5 §6:102-136）

| 类别 | 条目 | 闭合状态 |
| --- | --- | --- |
| 显式 require=0（22 处 / 18 个 mixin） | Q-REQ0-01（early/VanillaInventoryLimitMixin）+ Q-REQ0-02~22（late） | 全部登记，源码行号已核 22/22（t14.5:102-127）；**注入点缺失静默、实际生效面未在运行时验证（UNKNOWN，t14.1:501）** |
| 勘误 | Q-REQ0-CORR-01（t14.5:131） | 「SimpleInventoryMixin 缺省 require=1、Q-DUP-01 硬失败面」**不成立**：10.7 内嵌 0.8.7 fork 注入缺省 requiredCallbackCount=0（字节码证据，t14.5:41-52）；风险面修正为「双 JSON 装载 UNKNOWN + 静默失效无报告」 |
| 新增（t14.5:132-136） | Q-REQ0-NEW-01 | **核心不 fail-fast**：early 27 处无显式 require 的注入点同样缺省 0，refmap 失效/方法改名/混淆变化时核心注入点静默；违反「核心注入 fail-fast」目标 |
| 新增 | Q-REQ0-NEW-02 | **optional 缺失无日志无报告**：「optional 缺失无日志即成功」现状成立（loader 跳过零日志 + 注入点缺失零日志 + @Pseudo 目标缺失 DEBUG 级） |
| 新增 | Q-REQ0-NEW-03 | **配置未注册无检测**：`StackUpUpLateMixinLoader.kt:12` `?: return true` 未知配置无条件入队；无启动期双向校验 |
| 新增 | Q-REQ0-NEW-04 | **MixinToggles 非可配置**：12 项默认 true、无 GUI/命令/持久化、关不掉且关闭零日志 |
| 新增 | Q-REQ0-NEW-05 | **冲突禁用静默**：StackUpUpCore.kt:88-92 冲突时 early 配置不返回（空表），无 mixin 级日志、无机器报告 |

### 2.4 Q-DUP / Q-AMB（t14.1:504-518）

| 条目 | 内容 | 闭合状态 |
| --- | --- | --- |
| Q-DUP-01 | SimpleInventoryMixin 同时注册 cyclopscore.json 与 storagenetwork.json（t14.1:405-410,506-508） | 双配置同 mixin 双注册行为**未运行验证（UNKNOWN，t14.1:511）**；勘误后无 require=1 硬失败面（t14.5:131） |
| Q-DUP-02 | TileInventoryMixin 同时注册 mantle.json 与 storagenetwork.json（t14.1:419-424,509-510） | 同上 UNKNOWN（t14.1:511） |
| Q-AMB-01 | method="<init>*" 构造器通配且无 descriptor，5 处（Q-DESC-09/11/13/18/22）：第三方多含 intValue=64 构造器时 ModifyConstant 替换全部匹配点，require=0 静默多命中（t14.1:512-513） | 未闭合 UNKNOWN |
| Q-AMB-02 | 无 descriptor 普通方法注入（Q-DESC-08 addItems、Q-DESC-16 insertItem、Q-DESC-19 extract、Q-DESC-10/12/14/20 getInventoryStackLimit）：重载多命中冲突或空命中静默（t14.1:514-516） | 未闭合 UNKNOWN |
| 非问题项 | EnderIO 两 mixin 的 ()I 与 (I)I 已按 descriptor 区分；CommandGive/ReplaceItem @At target 已按 ()I 与有参重载区分（t14.1:517-518） | 闭合 |

### 2.5 OBS 观察项（t14.1:520-536）

- **OBS-01**：CDR 8.7 载「20 个配置 JSON（early 1 + late 19）」，实测 13 个（early 1 + late 12）——账本表述与当前树不符（t14.1:522-523,
  579）；引用 T14.0 结论按本表口径修正（t14.1:579）。
- **OBS-02**：已勘误（Q-REQ0-CORR-01，t14.5:131），不再有「require=1 硬失败面」表述。
- **OBS-03**：brandonscore.json 缺 minVersion 与 compatibilityLevel；cyclopscore.json 缺 compatibilityLevel；MixinConfig 对缺
  minVersion 配置有 WARN（t14.1:526-527；t14.5:89）。
- **OBS-04**：5 个 late JSON 含空 client/server 数组；均 @env(DEFAULT)，无环境侧实际影响（t14.1:528-529）。
- **OBS-05**：AppEngPatternTermMixin 反射路径违反 AGENTS 反射禁令（t14.1:530-531）→ 迁移 M3。
- **OBS-06**：ItemStackNbtMixin 三个 shadow（stackSize/item/stackTagCompound）无 refmap 条目；运行时 remap 是否闭合 UNKNOWN
  （t14.1:532-534；t14.3:54）。
- **OBS-07**：ServerRecipeBookHelperMixin 源码直书 srg 名 func_194324_a，混淆映射变更时注意（t14.1:535-536）。

### 2.6 UNKNOWN 账本汇总（未闭合证据，全部显式列出）

| 类别 | 条目 | 来源 |
| --- | --- | --- |
| 第三方签名 | Q-DESC-08~23（16 处，late） | t14.1:451-466,569 |
| 生效面 | Q-REQ0-01~22 实际注入命中/静默面未运行验证；缺省 require=0（early 27 处）静默面 | t14.1:501；t14.5:41-52,132 |
| 双注册 | Q-DUP-01/02 双配置同 mixin 装载行为 | t14.1:511 |
| 通配/重载 | Q-AMB-01/02 | t14.1:512-516 |
| shadow refmap | OBS-06（E12/E13 运行期判定受限） | t14.1:532-534；t14.2:103 |
| 字段面 | L07 @Shadow stackLimit 存在性 | t14.2:104；t14.3:58 |
| 链式化 | L01 operation 链跳过、链式化替代评估 | t14.2:114；t14.3:41 |
| 配置账本 | OBS-01 CDR 8.7 配置数差异 | t14.1:522-523,579 |
| 11.x 运行 | t14.4 UNKNOWN-A（初始化入口）/B（fork 补丁）/C（handler 命中）+ CDR 8.7 ①~⑥（minVersion、provider 初始化、manifest/required-after、CoremodClassFilter 前缀、无 TweakClass 启动路径、双 ASM） | t14.4:69-72,80；CDR:594-601 |
| 守恒运行回填 | T12.5 报告关闭态门：t14.6:12 记「T12.4/T12.5 未落地」，但 run/logs 已有守恒产物且 git 0c63b51 提交信息载「T12.5 schema 报告」——矛盾，**UNKNOWN，未以任何一方宣称闭合**（见 §5.5） | t14.6:12；§5.5 |
| 借鉴 | B1 网络同步（Forge 1.12.2 包通道、双端一致性）、B4 快照 version/hash、AE2 第三方写入链、上游守恒证据（无 src/test） | t14.6:59,115 |

## 3. MixinBooter 版本决定（11.13 必选迁移，**已实施**）

**决定**：目标版本 `zone.rong:mixinbooter:11.13`（2026-08-05 发布，坐标不变），依赖 cleanmix 0.7.1 + mixinextras-common 0.5.5
（Cleanroom fork，同包名无 relocation），无 TweakClass/FMLCorePluginContainsFMLMod，manifest 含 `MixinConfigs: mixin.mixinbooter.
init.json`（CDR 8.6:508-515；8.7:575-585）。**迁移已于 2026-08-09 实施**（提交 f193ff18，CDR 8.6.1）：当前
`gradle/libs.versions.toml:20 = "11.13"`；外部 mixinextras 0.5.0 声明已删；cleanmix 0.7.1 以纯编译期 AP 加入（refmap 修复）；
dev 运行（CleanMix 0.8.7 + MixinExtras 0.5.5）与 refmap 装载均已实证（CDR 8.6.1；复核通过）。

**迁移顺序（5 步，实施任务按序执行，CDR 8.6:528-535）**：
1. `gradle/libs.versions.toml:20` `mixinbooter = "10.7"` → `"11.13"`（版本值唯一一处；`build.gradle.kts:53` 字符串插值不变；
   8.1 所引 build.gradle.kts:434/438-439 行号已随 T15 过期，实际触点以本段为准）。
2. 删除外部 mixinextras 声明：`build.gradle.kts:57-58` compileOnly/annotationProcessor 与 `gradle/libs.versions.toml:21,29`
   `mixinextrasCommon` 条目（改用 11.13 内嵌 Cleanroom fork 0.5.5；17 个 mixin 文件的 `com.llamalad7.mixinextras.*` import
   同包名不变）。
3. 编译验证：记录 deprecation 警告集合（IEarly/ILateMixinLoader @Deprecated），跑 `test` 与 `spotlessCheck`。
4. `runServerAutoTest` 全量回归 + `CoremodHierarchyBytecodeSafetyTest`/`EarlyMixinBytecodeSafetyTest`/`MixinBooterIntegrationTest`。
5. T14.7 准入门复核，实施任务不自宣通过。

**回滚边界（CDR 8.6:536-538）**：步骤①单值回退（toml:20 改回 "10.7"）；步骤②两处恢复（build.gradle.kts:57-58 与 toml:21,29）；
两步相互独立、可分别回退；按 jj 工作区快照语义处理，不执行 git restore/git reset 等清理命令，不覆盖既有改动。

**触发条件（CDR 8.6:539-540）**：T9-R1 取证已闭合、决策记录生效、T14.0-T14.6 复核完成且 T14.7 通过后，创建独立迁移实现任务
（build.gradle.kts 红线例外，任务清单:451,582）。

**停止条件（任一命中即停止，CDR 8.6:541-543）**：provider/注册/refmap/classloader/容量证据未闭合；`runServerAutoTest` 或字节码
护栏失败；删除外部 mixinextras 后 `com.llamalad7.mixinextras.*` import 编译解析失败；11.13 内嵌 MixinExtras 运行时 bootstrap
无启动日志证据。迁移后停止条件 = 11.13 启动日志（bootstrap 证据 + 全部配置装载 + 无 minVersion 错误 + refmap remap + 注入器注册 +
无 InvalidInjection，t14.4:80）。

**11.x 侧证据状态**：迁移前仅静态（jar/POM/manifest/在线核验/官方 README/DeepWiki 佐证），**全部运行行为 UNKNOWN**（t14.4:79；CDR
8.7:603 无源码不可判定）；10.7 侧有 jar + 双端运行日志双重证据（t14.4:78）。

## 4. 未知 jar 台账（12 个 mod，无源码不可判定）

T2a §5（t2a:156-161）：late 目标 12 个模组 jar 均缺失（文件名/版本未登记，不得补猜）：

| # | modId | 对应 late mixin 目标数 | 受影响注入点 |
| --- | --- | --- | --- |
| 1 | appliedenergistics2（AE2） | 4 mixin（L01-L06 目标） | Q-DESC-08~13 |
| 2 | actuallyadditions | 1（TileEntityInventoryBaseMixin L20） | Q-DESC-21 |
| 3 | brandonscore | 1（BrandonsCoreInventoryLimitMixin L07/L08） | Q-DESC-14、L07 字段面 |
| 4 | cyclopscore | 1（SimpleInventoryMixin L19，另 storagenetwork 复用） | Q-DESC-20、Q-DUP-01 |
| 5 | enderio | 2（EnderIO×2 L10/L11，descriptor 已闭合） | — |
| 6 | ic2 | 1（InvSlotMixin L15） | Q-DESC-18 |
| 7 | mantle | 1（TileInventoryMixin L21，另 storagenetwork 复用） | Q-DESC-22、Q-DUP-02 |
| 8 | refinedstorage | 3（ItemGridHandler×2 L16/L17 + NetworkNodeStorageMonitor L18） | Q-DESC-19（L16/L17 已闭合） |
| 9 | storagenetwork | 复用 SimpleInventoryMixin/TileInventoryMixin | Q-DUP-01/02 |
| 10 | integrateddynamics | 1（TileMechanicalMachineMixin L22） | Q-DESC-23 |
| 11 | limelib | 1（CommonTileInventoryMixin L09，descriptor 已闭合） | — |
| 12 | immersiveengineering | 2（IEInventoryHandler L12/L13 + IEMachineSlotLimit L14，16 目标） | Q-DESC-15/16/17 |

当前工作副本 `run/mods/`、`local-dev-mods/` 均为空（gitignored），无任何可读第三方字节码/源码（t2a:158-161）。Forge 自身 wrapper
与 vanilla 反编译源码不属缺失项（t2a:161）。影响：late 全部目标与方法签名**无源码不可判定**；Q-DESC-08~23 未闭合，禁止补
descriptor；Q-REQ0-02~22 与 Q-DESC-08~23 的注入点命中/静默面未运行验证（t14.1:569；t14.5:163）。另：本地缓存无
com.cleanroommc:*、无 mixinbooter/11.13（在线核验，未运行加载矩阵，t14.4:74）。

## 5. 验证清单（客户端/服务端/混淆/字节码/守恒）

状态口径：**通过**=已有运行/产物/字节码证据闭合；**未执行**=本次 T14 审计未运行；**UNKNOWN**=无证据或证据不足。如实标注，
不以静态检查冒充运行测试（任务清单:583）。

### 5.1 已执行并有证据（通过）

| 类别 | 项目 | 状态 | 证据 |
| --- | --- | --- | --- |
| 配置 | JSON↔类双向 38/38 全对齐，Q-UNREG 无 | 通过 | t14.1:35-81,437-438 |
| 配置 | src JSON ↔ build JSON 13/13 逐字节 SAME；refmap 双产物 md5 相同 | 通过 | t14.1:5-7；t14.4:52 |
| 字节码 | build/classes early/late 各 20/18 .class 与源一一对应 | 通过 | t14.1:80-81 |
| 混淆/refmap | early 16/20 有条目、缺 4 个全部 remap=false（预期一致）；late 18 全部 remap=false 无条目（预期一致）；refmap method 解析与源码 @At 逐项一致 | 通过 | t14.1:555-560 |
| 运行时（客户端/服务端） | 10.7 双端运行日志：Mixin 0.8.7 Service=LaunchWrapper、MixinExtrasService 单例协商、refmap remap、early 配置装载（latest.log:59-60）、late loader 零日志跳过（latest.log:1054，SERVER 2026-08-08-1.log.gz 同构） | 通过 | t14.4:18-30,54；t14.5:56-58,157 |
| 字节码（运行时语义） | javap 反汇编 mixinbooter-10.7.jar 9 个类：注入缺省 require=0、loader 跳过零日志、@Pseudo 缺失 DEBUG、minVersion WARN | 通过 | t14.5:151-156 |
| 注入面 | 注入类型分布 50 处→49 处核对；@Redirect 4 处逐条核对 vanilla 调用点（EntityItem.java:258、InventoryPlayer.java:69/341/343/346/348） | 通过 | t14.1:561-562；t14.2:116-120；t14.3:10 |
| 守恒（静态/测试） | ConservationAuditor/ConservationEvent 只读审计、公式 storedDelta+remainderCount==offered、simulate 不产事件；ConservationAuditorTest/ConservationAuditorBytecodeTest 存在；T12.3 接入点 2 处 | 通过（逻辑闭合） | t14.3:45-50；t14.6:50-52 |
| 守恒（运行时产物） | run/logs/stackupup-conservation-summary.json + stackupup-conservation.jsonl 存在（3 事件：1 simulate + 1 不平衡真实事件，handler=example.TruncatingThirdPartyHandler，callSite=Ae2ItemHandlerInsertLimiter#insertCapped——疑似 dev 探针合成名） | 产物存在；**报告关闭态门判定 UNKNOWN（见 5.5）** | 本收口核对 |
| 借鉴准入 | 8 项裁决：6 否决/1 待证据/1 部分；S1-S4/B1-B4 逐项回链上游 file:line | 通过（审计闭合） | t14.6:16-28,95-108 |

### 5.2 测试与运行验证清单（未执行项如实标注，不得写成通过）

| 类别 | 项目 | 状态 | 说明 |
| --- | --- | --- | --- |
| 测试 | `CoremodHierarchyBytecodeSafetyTest` | **已执行/通过** | 协调者波次7 BUILD SUCCESSFUL；build/test-results/test/TEST-io.alexjoest.stackupup.core.CoremodHierarchyBytecodeSafetyTest.xml（1 用例 0 失败，时间戳 2026-08-09 03:32 +08） |
| 测试 | `EarlyMixinBytecodeSafetyTest` | **已执行/通过** | 同上波次7；TEST-io.alexjoest.stackupup.mixin.EarlyMixinBytecodeSafetyTest.xml（2 用例 0 失败，时间戳 2026-08-09 03:32 +08） |
| 测试 | `MixinBooterIntegrationTest` | **已执行/通过** | 同上波次7；TEST-io.alexjoest.stackupup.mixin.MixinBooterIntegrationTest.xml（4 用例 0 失败，时间戳 2026-08-09 03:32 +08）；三护栏合计 7 用例 0 失败；时间戳以最新 build/test-results 为准（复核时点 2026-08-09 04:38 +08 全量运行，用例数 1/2/4 与 0 失败不变） |
| 测试 | 完整 `test` / `spotlessCheck` | 未执行 | 本次 T14 审计未运行 |
| 回归 | `runServerAutoTest`（自动验收矩阵） | 未执行 | 本次 T14 审计未运行；T14.5 记「当前实现仍有异常分类缺口，不构成已通过证明」（t14.5 引用 AGENTS） |
| 运行矩阵 | 第三方 mod 运行矩阵（12 mods） | 未执行 | jar 缺失（§4）→ late 注入命中面 UNKNOWN（t14.5:158,163） |
| 运行 | 11.x 启动/加载矩阵 | 未执行 | 11.13/0.7.1/0.5.5 候选 jar 仅在线核验，未保留本地副本、未运行（t14.4:74；CDR 8.7:603） |
| 运行 | 负向运行验证（T14.5 停止条件 4） | **已执行** | t14.5 §12：缺省 require=0 静默（10.7 边界）、显式 require=1 缺失产生 WARN 可定位日志；停止条件 4 部分关闭 |
| 混淆 | OBS-06 三个 shadow 无 refmap 条目的运行期 remap 验证 | 未执行 | t14.1:532-534,565 |
| 行为测试 | H1 组（E06/E12/E13/E16/E17/E18/E19）行为测试门 | 未执行 | t14.3:65「测试门先行，通过前不评估形态迁移」；t14.5:75 |
| 守恒 | T12.5 守恒运行时报告回填/关闭态门 | 未执行/UNKNOWN | 见 5.5 |

### 5.3 高风险项（H1-H6，t14.3:65）——全部未闭合，不得标为通过

H1 完整重写式 HEAD 接管 6 处（E06/E13/E16/E17/E18/E19，**无行为测试**）；H2 E12 构造中途锚点；H3 E13 NBT schema 变化跨端相等性；
H4 L01 未知 handler 分块非原子 + operation 链跳过；H5 E16 双端规则一致性；H6 L06 反射实现（违反反射禁令）。

### 5.4 T14.5 停止条件命中状态（t14.5:167）——四项全部命中，收敛不得宣称通过

1. 核心注入不 fail-fast（缺省 require=0，Q-REQ0-NEW-01）；2. optional 缺失无日志（Q-REQ0-NEW-02）；3. 配置未注册无检测
（Q-REQ0-NEW-03）；4. 默认 require=0 掩盖错误（Q-REQ0-NEW-01 同源）。契约建议（core fail-fast 门、optional 缺失日志字段、
机器报告 config/stackupup/mixin-report.json、双向校验）见 t14.5:67-75，**未实施**。

### 5.5 守恒/T12.5 状态矛盾（如实记录，UNKNOWN）

- t14.6:12 载「T12.4/T12.5 未落地」；t14.3:60 载「T12.5 未闭合，T13 维持 BLOCKED」。
- 但工作副本 run/logs/ 存在守恒产物（5.1 行）且 git 提交 0c63b51（2026-08-09 03:27）信息载「波次6：T12.5 schema 报告 +
  T14.5 配置诊断审计 + 独立复核通过」。
- 审计文档基线（2026-08-08）与提交/产物存在时序与口径矛盾：**T12.5 报告关闭态门状态 UNKNOWN**，本矩阵不据此宣称守恒运行回填
  已闭合；T13 维持 BLOCKED（任务清单:22,592）。

## 6. 后续独立实现任务规格（M1/M2 已完成；其余 T14.7 通过后创建）

| 任务 | 范围 | 前置条件 | 验证/停止条件 | 证据 |
| --- | --- | --- | --- | --- |
| M1 | E04 EntityItemMergeMixin @Redirect → @ModifyExpressionValue | **已完成**（提交 5e099ab1，独立复核通过；实施时序先于 T14.7 准入门，属协调者裁决接受——证据闭合：@Redirect 无保留证据、参数序三重验证、P0 事实 c 调用方保持 2、全量测试 0 失败） | 门控等价（count1+count2 > max(...) 判定与合并落库不变）；不新增 @Redirect | t14.2:21,94-98；t14.3:21,68 |
| M2 | E07/E08/E09 InventoryPlayerAddResourceMixin @Redirect ×3 → @ModifyExpressionValue | **已完成**（提交 5e099ab1，独立复核通过；实施时序先于 T14.7 准入门，属协调者裁决接受——证据闭合：@Redirect 无保留证据、参数序三重验证、P0 事实 c 调用方保持 2、全量测试 0 失败） | 调用方计数不变；门控与夹取行为不变；addResource 返回语义不变 | t14.2:24-26,96-97；t14.3:22-24,69 |
| M3 | L06 AppEngPatternTermMixin 反射实现 → @Shadow/@Accessor | **Q-DESC-13 闭合 + 对应 AE2 jar 在案**（当前 BLOCKED，t14.2:113；t14.3:57） | 无反射调用（AGENTS 反射禁令）；构造 RETURN 注入语义保持 | t14.1:530-531；t14.2:56,113 |
| H1 组测试门 | E06/E12/E13/E16/E17/E18/E19 行为测试（含 H2 构造锚点、H3 schema 跨端相等、H5 双端一致性） | 无（测试门先行） | 行为测试通过前不评估形态迁移（t14.3:65,71）；不得用源码 contains 冒充行为验证 | t14.3:65,71；t14.5:75 |
| L01 链式化 | AppEngAdaptorItemHandlerMixin operation 链化评估 | **UNKNOWN，不迁移**（Q-DESC-08 未闭合 + 无源码） | 不生成迁移任务 | t14.2:114；t14.3:41,73 |
| 配置诊断收敛 | core fail-fast 门 + optional 缺失日志 + 机器报告 + 配置未注册双向校验（t14.5 §3 契约） | **已完成主体**（t14.5 §11：核心 fail-fast/optional 日志/未注册检测 3 项停止条件关闭；第 4 项部分关闭——缺省 require=0 静默为 10.7 边界，显式 require=1 缺失产生 WARN 日志，t14.5 §12） | 报告字段契约（configName/modId/targetClass/injectionPoint/requireValue/resolution/timestamp/side）；core 层缺失 ERROR+中止（可关闭开关显式 WARN） | t14.5:67-75,167 |
| MixinBooter 11.13 迁移 | CDR 8.6 五步（§3） | **已完成**（提交 f193ff18，CDR 8.6.1；独立复核通过） | 剩余 UNKNOWN：生产混淆（notch）环境 refmap 消费、ASM 5.2/9.8 并存审计 | CDR 8.6:528-543；8.6.1 |
| 借鉴准入实施 | 无（B1 待证据不生成任务；S1-S4/B2/B3/B4 永久否决） | B1 若获准须满足 5 项准入条件，实施在 T14.7 门后 | t14.6:60,110-115 | t14.6:59-60,114 |

## 7. 交接边界

1. **T14.7 通过前不解锁**：MixinBooter 11 迁移与其余生产注入迁移（M3 等）保持锁定（CDR 8.6:483-484,539-543；
   任务清单:465,582,586）。
2. **T14.7 通过条件**：T14.1-T14.6 独立复核完成 + 本矩阵经 Terra 复核容量/守恒门 + 运行时/字节码代理分别复核客户端/服务端与
   refmap + §5.2 未执行项按职责由相应代理补齐或显式确认（任务清单:585）。
3. **复核状态（未完成，如实记录）**：t14.1:584、t14.2:134、t14.3:77、t14.4 同构、t14.5:176、t14.6:119 复核记录均为「待指派」；
   而 git 提交 0c63b51 信息载「独立复核通过」——**提交信息与文档记录矛盾，按文档口径记为待复核**，不因提交信息宣称已复核。
4. **解锁范围**：T14.7 通过仅解锁 T13 注入生态前置门与 MixinBooter 11 迁移实现任务创建权（由 T9-R3/T15 红线例外授权）；
   不解锁其他生产迁移（任务清单:586）。
5. **已知未闭合项移交**：Q-DESC-08~23、Q-REQ0-NEW-01~05、Q-DUP-01/02、Q-AMB-01/02、OBS-06、H1-H6、11.x UNKNOWN-A/B/C +
   ①~⑥、T12.5 关闭态、OBS-01 账本修正——全部保留 UNKNOWN/未闭合，移交 T14.7 复核与后续任务，不得在下游改写成通过。

## 8. AGENTS 六栏输出

1. **状态**：`PARTIAL`——本文档完成六份审计文档 + CDR 8.6/8.7 + T2a §5 的汇总收口，矩阵逐项回链来源行号；但 (a) T14.1-T14.6 全部
   复核记录为「待指派」（与 git 提交信息矛盾，按文档口径待复核）；(b) 三个字节码护栏测试已通过（见 §5.2）；完整
   test/spotlessCheck/runServerAutoTest 未执行；(c) 12 个第三方 mod jar 缺失，Q-DESC-08~23 未闭合；(d) T14.5 停止条件 3 项已关闭、
   第 4 项部分关闭（缺省 require=0 静默为 10.7 边界，t14.5 §12 负向验证）。迁移准入门**未通过**，不得宣称
   `PASS`，不解锁 MixinBooter 11 迁移与 M3 等剩余迁移（M1/M2 已由协调者裁决接受为已完成）。
2. **实际变更**：仅新建 `docs/agent/t14.7-发布前矩阵.md`（本文件）。未修改任何 JSON、Mixin、loader、配置、build 文件、测试、
   生产代码或其它文档（工作副本既有未提交改动 `docs/agent/重构任务清单.md` 未触碰）。
3. **证据**：t14.1（38/38 双向登记、Q 表、OBS）、t14.2（50→49 分类、M1/M2/M3）、t14.3（49 注入点、H1-H6、E25 勘误）、t14.4
   （10.7 双端日志闭合、11.x UNKNOWN-A/B/C）、t14.5（缺省 require=0 字节码证据、Q-REQ0-NEW 条目、四项停止条件）、t14.6（6 否决/
   1 待证据/1 部分）、CDR 8.6/8.7（11.13 五步迁移、回滚、触发/停止、UNKNOWN ①~⑥）、T2a §5（12 jar 台账）、任务清单:575-586
   （T14.7 定义）；本收口补充核对：三个字节码护栏测试文件存在且已执行（build/test-results 三份 XML 共 7 用例 0 失败，
   2026-08-09 03:32 +08，见 §5.2）、run/logs 守恒产物存在、git 0c63b51 提交信息、工作副本仅
   `重构任务清单.md` 有未提交改动。
4. **验证**：已执行=全部来源文档通读并按行号交叉提取（§1-§7 每行回链）；文件存在性核对（三个护栏测试、ConservationAuditor 系、
   run/logs 产物）；git 状态核对；三个字节码护栏测试运行产物核对（build/test-results，见 §5.2）。未执行=§5.2 其余测试/运行项
   （如实标注，未包装为通过）。
5. **剩余风险**：(a) 独立复核全部未完成，本汇总不构成 T14.7 准入门通过；(b) 12 mod jar 缺失 → late 签名/生效面 UNKNOWN；(c) 11.x
   运行行为全 UNKNOWN；(d) T12.5 关闭态矛盾（t14.6 记未落地 vs 提交/产物存在）未裁决；(e) git 0c63b51「独立复核通过」与文档
   「待指派」矛盾未裁决；(f) OBS-01 账本修正待 T14.0 口径联动；(g) §5.2 未执行项需由运行时/字节码代理按职责补齐。
6. **复核**：作者=文档收口代理（本任务书明确作者不得自审）；独立复核**待指派**（Terra 复核容量/守恒门，运行时与字节码代理分别复核
   客户端/服务端和 refmap，任务清单:585）；未完成独立复核前不得宣称 PASS。

## 9. 复核记录

- 作者：文档收口代理（T14.7 汇总部分）；只读汇总 + 本文档写入；未修改任何 JSON/Mixin/loader/配置/生产代码。
- 独立复核：待指派。复核抽查项建议：注入器决策矩阵 49 处与 t14.2/t14.3 逐行对齐；Q 表闭合状态与 t14.1/t14.5 一致；11.13 迁移
  顺序/回滚/停止条件与 CDR 8.6 逐字核对；12 jar 台账与 T2a §5 一致；§5 已执行/未执行划分无夸大。
