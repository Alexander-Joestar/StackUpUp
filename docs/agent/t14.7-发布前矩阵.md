# T14.7 已知限制清单

> 这是个人玩具项目的**备忘**，不是发布门。别把它当准入条件，也别指望谁先签字才能干活。
> 下面写的是当前真实存在的限制和没做完的事，目的只是下次别忘了。

## 已知限制

| 限制 | 现状 | 说明 |
| --- | --- | --- |
| 第三方 mod jar 缺失 | 没测过 | 目标 jar 不在手上，真实写入路径没有源码，属"无源码不可判定"。别按类名猜容量 |
| 生产混淆（notch）refmap 消费 | 没实测 | 编译期 CleanMix AP 能生成 refmap，但没在 notch 环境真跑过消费；dev/SRG 的证据不能当 notch 证据 |
| 真实客户端 F3+T | 没做完 | 第二次重载是外部操作者手动触发的，自动化注入没触发过。见 `t8.0-真实f3t基线记录.md` |
| 高风险 Mixin 的运行时行为 | 没验证 | 结构检查和服务端启动不能代替实际客户端行为 |
| T8.2 重载重新注入 | 没实现 | 卡在真实 F3+T 基线上 |
| T16 ForgeDevEnv 对齐 | 还没开始 | 依赖、wrapper 和发布项都还没动 |

## 当前构建与注册事实

- `gradle/libs.versions.toml:20-23,29-31`：MixinBooter `11.13`、CleanMix `0.7.1`。
- `build.gradle.kts` 的 useMixins 块（:49-66）：CleanMix 作为编译期 annotation processor 挂载，负责 obfuscation AP / refmap。
- `src/main/kotlin/io/alexjoest/stackupup/bootstrap/StackUpUpMixinConnector.kt`：普通 `class StackUpUpMixinConnector : IMixinConnector`，通过 manifest `MixinConnector` 注册，统一做 early/late 配置校验、冲突处理、mod presence 和 toggle 决策。
- MixinBooter 10.7、`IEarlyMixinLoader`/`ILateMixinLoader`、旧 loader 和旧 JSONL 守恒报告都只是历史，不是现在的实现。

## 跑过什么

| 检查 | 上次结果 | 备注 |
| --- | --- | --- |
| `compileJava`、CleanMix obfuscation AP、refmap 生成 | 通过 | CDR §8.6.1。生产 notch 消费仍然没实测 |
| `test` 与三项字节码护栏 | 通过 | CDR §8.8 |
| `spotlessCheck` | 通过（2026-09-14 实测） | 本 lane 实测 BUILD SUCCESSFUL；CDR §8.8 记的 `RuleFieldContextProvider.kt` 预存违规现在已不再命中 |
| `runServerAutoTestMatrix` | 通过（2026-08-10） | `run/logs/autotest-report.txt` PASS，早先两个转发 wrapper 探针的失败记录已按 CDR §3.12 修正预期。**此后代码改过，没重跑** |
| 真实客户端 F3+T | 没做完 | 见上面的已知限制 |
| 第三方 mod 矩阵、生产混淆 refmap 消费、高风险 Mixin 客户端行为 | 没做 | 见上面的已知限制 |

已记录的 11.13 证据：CleanMix AP 生成 refmap、dev 服务端发现并加载 connector、MixinExtras 0.5.5 初始化和 refmap remap。链路见 `compatibility-decision-record.md` §8.6.1「迁移实施记录」与 §8.8「IMixinConnector 装载入口迁移」。

## T12 方案 A 的事

方案 A 已归档：移除了 AE2 热路径 wrapper 和写入后补偿，保留原 `insertItem` 调用和 Forge remainder 契约。守恒 JSONL 报告、审计器及其关闭态门也一起归档了，现在的运行验证靠真实行为测试和边界探针。老 JSONL 报告不代表现在的实现。

第三方内部写入链没有源码，容量上依然保持"无源码不可判定"。
