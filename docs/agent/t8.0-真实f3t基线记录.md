# T8.0 真实 F3+T 重载基线 — 环境可行性质检记录

- 日期：2026-08-08（客户端会话 23:13–23:15）
- 任务类型：调查 + 环境可行性质检（T8.0 前置门；不宣称 F3+T 已完成）
- 目标：确认「真实启动客户端 → 执行 F3+T → 记录重载前后状态」在本机是否可行；本次只交付环境可行性质检与基线记录，F3+T 按键操作本身未执行。
- 写租约：`docs/agent/t8.0-真实f3t基线记录.md`（新建）；未修改任何生产代码、规则消息或语言注入。

## 1. 环境检查结果（只读）

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 最近一次启动日志 | 23:11–23:12 为**服务端**自动验收矩阵启动（`FMLServerTweaker`/`nogui`/`DedicatedServer`），成功完成：`Done (19.021s)!` | `run/logs/latest.log:758`（23:12:29） |
| eula.txt | 存在，内容 `eula=true`（自动验收模板生成） | `run/eula.txt` |
| 客户端资产 | `run/assets` 不存在；资产位于 Gradle 缓存 `C:\Users\25761\.gradle\caches\retro_futura_gradle\assets`（本次启动 `:downloadVanillaAssets` 任务实际执行，网络可用） | `logs/t8.0-runclient.out:74,83`（GradleStart 参数 `--assetsDir`）；本次 `:downloadVanillaAssets` 非 SKIPPED |
| 客户端窗口/进程 | 无残留 Minecraft 进程；启动时无端口冲突 | `tasklist`（启动前无 30188 进程） |
| 已知非致命错误 | 与 P0 记录一致：FML Maven 目录格式错误、`FML appears to be missing any signature data`、Forgelin/Kotlin 多发行 jar 的 `META-INF/versions/9` 条目 `probably a corrupt zip`（ASM 5.2 无法解析 Java 9+ class 文件）。均不影响加载，8 个 mod 成功加载 | `run/logs/latest.log:18-20,56,95,256,351,446,...` |

## 2. 启动执行结果（runClient）

- 命令：`JAVA_HOME=/c/Users/25761/.jdks/jbr-25.0.4 ./gradlew --no-daemon runClient`（后台，输出重定向 `logs/t8.0-runclient.out`）。
- 结论：**成功启动并进入主菜单**，随后被外部交互接管，最终优雅退出，进程退出码 0。无人值守启动本身可行。
- 时间线（取自 `run/logs/latest.log` 客户端会话与 `logs/t8.0-runclient.out`）：
  - 23:14:23 客户端窗口创建（`Get-Process` 窗口标题 `Minecraft 1.12.2`，PID 30188）
  - 23:14:28 `Setting user: Developer`；23:14:31 `LWJGL Version: 2.9.4`
  - 23:14:39 `Reloading ResourceManager: Default, FMLFileResourcePack:...`（启动期唯一一次资源重载，见第 3 节）
  - 23:14:47 `Forge Mod Loader has successfully loaded 8 mods`；`StackUpUp: Loaded 0 DSL rules from ...\config\stackupup\main.su`
  - 23:14:53 Realms 检查线程运行 → **主菜单已显示**
  - 23:14:54–23:15:05 外部交互创建并进入单机世界 `New World`（`run/saves/New World/` 存在，23:14 创建）
  - 23:15:20–23:23 聊天区出现 `/stackupup` 相关记录（TAB 补全建议 `/stackupup, /stats`、`edit, reload, state` 及 `Usage: /stackupup <reload|edit|state>`）
  - 23:15:33–34 优雅退出（`Saving and pausing game...` → `Stopping!` → SoundSystem 关闭），Gradle 任务退出码 0
- JAVA_HOME 说明：jbr-25.0.4 仅用于 Gradle daemon（Gradle 9.4 可运行）；**客户端实际由 RFG Java 8 toolchain 启动**（栈跟踪均为 `1.8.0_492`，temurin-8）。任务指定命令已按原样执行，未更换客户端 JVM。

## 3. F3+T 未执行的真实原因与后续方案

- **真实原因**：无被指派的人工操作员执行 F3+T 按键；本次为无人值守启动验证。会话全程仅出现**一次** `Reloading ResourceManager`（23:14:39 启动期），若执行过 F3+T 会出现第二次同格式重载记录 —— 因此可确认本会话未执行 F3+T。
- **判定判据（后续可用）**：F3+T 在本环境会产生 `SimpleReloadableResourceManager: Reloading ResourceManager: ...` 日志行（与启动行同格式，`run/logs/latest.log` 已证明该日志链路活跃），可作为 T8.1 的完成证据；配合 `debug.log` 交叉核对。
- **外部交互事实与归属**：23:14:54 起的建世界与 `/stackupup` 聊天输入、23:15:33 的退出，与本次运行并发出现；本任务未注入任何键盘/鼠标输入。`runClient` 未带 `-PstackupupDevAutoTest*`，autoTest JVM 参数未注入（`DevAutomationConfig.enabled` 默认 false，且实际世界名 `New World` ≠ 驱动默认 `StackUpUp 自动测试`），排除本项目自动化驱动所为。同一时段工作区出现并发修改（`VanillaInventoryWriteMixin.java` 被删、`VanillaInventoryLimitMixin.java`/`StackLimitHooks.kt` 等被改，jj commit `1eac1aa6 → 3b37ff0c`），与游戏窗口人工式输入模式（1.12.2 TAB 补全回显）一致。**交互来源无法仅凭日志判定，标记 UNKNOWN**；但它证明本机可以向游戏窗口交付键盘输入，且存在可执行 F3+T 的人工操作者（需在 T8.1 正式安排）。
- **后续方案**：
  1. 人工操作：由明确的操作员在客户端窗口按 F3+T，以第二次 `Reloading ResourceManager` 日志行为完成判据（首选，判据明确、零风险）。
  2. 自动化键盘注入：可行性 **UNKNOWN**。本项目无现成键盘注入设施；Windows `SendKeys` 对游戏窗口（尤其非前台/全屏）键事件是否生效未验证，不能写成可行。T8.1 需先做注入方式实验并记录结果，不得以推测代替实验。
- **重载前后对比数据**：本次未采集（F3+T 未执行）。T8.1 应记录同语言代码下重载前后：自有消息解析/注入/缓存状态、`RuleRuntime` 快照是否保持、`main.su` 规则计数等，本次无任何对比数据可写，不得补全。

## 4. 结论状态：PARTIAL

- 环境可行性已确认：客户端可无人值守启动进入主菜单，日志链路完整，资产/网络/窗口输入均可用，无阻塞点。
- F3+T 按键操作未完成（无第二次资源重载记录），故不宣称 T8.0 通过；本记录仅交付可行性基线。

## 5. 输出模板六栏

1. **状态**：`PARTIAL`。环境可行已确认（客户端成功进入主菜单、日志证据完整）；F3+T 操作未执行，属明确未完成项。
2. **实际变更**：
   - 新建 `docs/agent/t8.0-真实f3t基线记录.md`（本文档）。
   - 运行副产物：`logs/t8.0-runclient.out`（runClient 控制台捕获，gitignored，非租约文件）；`run/saves/New World/`（游戏运行生成，非本任务主动写入）；`run/logs/latest.log`、`run/logs/debug.log` 被本次客户端会话追加（日志文件，非源码）。
   - 未修改任何生产代码、`RuleMessages`、语言注入或规则文件。
3. **证据**：
   - `run/logs/latest.log:75`（`Setting user: Developer`）、`:1057`（启动期唯一 `Reloading ResourceManager`）、`:1087`（`successfully loaded 8 mods`）、`:1086`（`Loaded 0 DSL rules from main.su`）、`:1089`（Realms 检查，主菜单证据）、`:1090-1131`（`New World` 建世界/加入/聊天/退出序列：`:1090` 启动集成服务器、`:1110` `Developer joined the game`、`:1118` `Usage: /stackupup`、`:1131` `Stopping!`）。
   - `logs/t8.0-runclient.out:74,83`（`--assetsDir` Gradle 缓存路径）、`:74`（`:downloadVanillaAssets` 执行）。
   - `run/eula.txt`（`eula=true`）。
   - `DevAutomationConfig.kt:58-64`（`enabled` 默认 false，`readEnabled("false")`）；`DevAutomationClientDriver.kt:51-70`（世界名应为 `StackUpUp 自动测试`，与观察到的 `New World` 不符）；`build-logic/convention/src/main/kotlin/autoTest.gradle.kts`（仅任务名或 `-PstackupupDevAutoTest=true` 时注入 `-D`，本次未命中）。
4. **验证**：
   - 已执行：`JAVA_HOME=/c/Users/25761/.jdks/jbr-25.0.4 ./gradlew --no-daemon runClient`（后台），退出码 0；`Get-Process` 观察到窗口标题 `Minecraft 1.12.2`；日志关键行核对如上。
   - 通过判据：启动进入主菜单 ✓；无 FATAL/Crash report ✓；进程存活 71 秒后优雅退出 ✓。
   - 未执行：F3+T 按键（无操作员指派，本次不执行）。
5. **剩余风险**：
   - F3+T 操作本身未完成；T8.1 需安排人工操作员或先实验自动化注入（可行性 UNKNOWN）。
   - 23:14:54 起的外部交互来源 UNKNOWN（疑似本机人工/其他会话，日志无法判定）。
   - 本记录撰写期间工作区存在并发修改（`VanillaInventoryWriteMixin.java` 删除、`StackLimitHooks.kt` 等修改），不属于本次变更；T8.1 执行前需复核当前快照下的行为基线，避免把并发修改写进本基线。
   - `run/assets` 缺失但资产经 Gradle 缓存解析，属正常 RFG 布局；若缓存被清空需重新 `downloadVanillaAssets`（网络依赖）。
6. **复核**：未完成独立复核（本记录为运行验证代理产出，待主代理指派非作者复核后更新本栏）。

## 6. T8.0 剩余部分：自动化 F3+T 注入可行性实验（2026-08-09 03:44–03:56，本机时钟已过 2026-08-08 午夜）

- 任务：探索自动化键盘注入可行性并实际尝试触发 F3+T；判据沿用本文档第 3 节（日志出现第二次 `Reloading ResourceManager`）。
- 关键环境事实：Windows 会话处于**锁定**状态——前台窗口全程为 `Windows 默认锁屏界面`（LockApp.exe，PID 22344），03:44–03:56 多次复检一致。这是全部注入尝试失败的根因。

### 6.1 执行过程

1. 后台启动 `JAVA_HOME=/c/Users/25761/.jdks/jbr-25.0.4 ./gradlew --no-daemon runClient`（任务 id `exec_e71a2fe6-…`，03:44 启动）；客户端 03:47:55 `Setting user: Developer`，03:48:09 `successfully loaded 8 mods`，03:48:10 Realms 检查（主菜单已显示）。
2. 等待主菜单稳定后依次实施 4 种注入方案，每种方案后核对 `run/logs/latest.log` 与 `debug.log`。

### 6.2 注入尝试与结果

| # | 方案 | 实现 | 结果 |
| --- | --- | --- | --- |
| 1 | 系统级键盘注入（keybd_event） | Python ctypes `keybd_event`：F3 按下 → T 按下 → T 抬起 → F3 抬起；前置 `SetForegroundWindow`（Alt 技巧） | 焦点抢占失败（`SetForegroundWindow` ret=0，前台仍为锁屏），键事件送达锁屏，无第二次 Reloading |
| 2 | AttachThreadInput + SW_RESTORE + SetForegroundWindow（两次重试） | 线程输入队列附加后 `BringWindowToTop` + `SetForegroundWindow` | 前台仍为锁屏（`GetForegroundWindow` 恒为锁屏句柄），`FOCUS FAILED`，无重载 |
| 3 | WScript.Shell COM `AppActivate` | PowerShell `$wshell.AppActivate("Minecraft 1.12.2")` | 返回 `True` 但前台未改变（仍为 `Windows 默认锁屏界面`），无重载 |
| 4 | 直投窗口消息（不经焦点） | `PostMessageW` `WM_KEYDOWN`/`WM_KEYUP`（VK_F3=0x72、VK_T=0x54，scan code 61/20，lParam 按位组装）；变体 A：先投 `WM_ACTIVATEAPP(TRUE)`/`WM_ACTIVATE(WA_ACTIVE)`/`WM_SETFOCUS` 再投键；变体 B：`SendMessageW` 同步投递；变体 C：F3 长按 1.6s + 3 次 T 敲击 | 消息全部入队且被游戏线程处理（`SendMessageTimeoutW(WM_NULL)` 返回 1，证明客户端线程存活并泵送消息），但**始终无第二次 Reloading**；键事件未进入 LWJGL 键盘状态 |

- 未实施：`java.awt.Robot`/jshell——与方案 1 同为系统级注入，受同一前台（锁屏）约束，无独立信息量。`command -v` 确认无 autohotkey/pyautogui/xdotool，未安装任何新工具（红线遵守）。注入脚本全部经 stdin 一次性执行，未落盘。

### 6.3 阻塞根因与机制定位

- **根因**：会话锁定（LockApp 持前台）。锁屏状态下：(a) 系统级注入（keybd_event/SendKeys/Robot）投递到锁屏窗口；(b) 锁屏前台不可被普通进程抢占（`SetForegroundWindow`/`AppActivate` 均无效）；(c) 唯一剩余的直投通道（PostMessage/SendMessage 到游戏窗口）消息虽被处理，但 LWJGL2 未将无焦点窗口的键消息转发为游戏键盘事件。
- **LWJGL2 机制（本地证据）**：`lwjgl-2.9.4-nightly-20150209-sources.jar`（Gradle 缓存）中 `org/lwjgl/opengl/WindowsKeyboard.java` 证实键盘事件由 native WndProc 调用 `handleKey()` 事件驱动（Java 侧无焦点过滤），且 `releaseAll()` 注释明确「窗口失焦时释放全部按下的键」——native 侧存在焦点跟踪。native WndProc 是否按 `GetFocus` 过滤：本地无 native 源码（GitHub raw 404×2、API 限流），标记 **UNKNOWN**；实证结果（消息被处理而无键事件）与「native 侧焦点门控」一致。

### 6.4 判据结果与证据

- 判据：日志出现第二次 `Reloading ResourceManager` = F3+T 已触发。结果：**未触发**。
- `run/logs/latest.log` 全程仅一条 `Reloading ResourceManager`（`:1058`，03:48:03 启动期；末尾行为 `:1091` 03:48:10 Realms，之后无新行，文件共 1091 行）；`run/logs/debug.log:1983` 为同一条，latest/debug 交叉一致。
- 重载前后自有消息状态：无对比数据（未触发），不补全。
- 客户端窗口：PID 4420（java.exe，标题 `Minecraft 1.12.2`，hwnd `0x32F0FB2`），主菜单状态存活于整个实验期间。

### 6.5 清理与租约

- 后台 gradle 任务经 TaskStop 终止；客户端 JVM（PID 4420，主菜单、无存档）`taskkill //PID 4420 //F` 结束；`jj st` 工作区无改动。
- 未修改任何生产代码、规则、消息或语言注入；未安装任何工具；仅更新本文档（租约内）。

## 7. 本任务六栏输出（自动化可行性实验）

1. **状态**：`BLOCKED`。探索与尝试全部执行完毕并得出结论：**当前环境（Windows 会话锁定，LockApp 前台）下自动化键盘注入不可行**——系统级注入被锁屏接管，直投窗口消息被游戏线程处理但不产生键盘事件。F3+T 未触发（判据：无第二次 `Reloading ResourceManager`），不写成通过。原第 3 节「自动化注入可行性 UNKNOWN」据此更新为「锁定状态不可行，解锁状态未验证」。
2. **实际变更**：仅更新本文档（追加第 6、7 节）；未改任何其他路径。运行副产物：`run/logs/latest.log`、`run/logs/debug.log` 被本次客户端会话追加（日志文件，非源码）。
3. **证据**：`latest.log:75`（Setting user）、`:1058`（唯一 Reloading）、`:1088`（8 mods）、`:1091`（Realms 主菜单，文件末尾，03:48:10 后无新行）；`debug.log:1983`（同一条 Reloading）；LockApp 前台（PID 22344，`Get-Process` ProcessName=LockApp，`GetForegroundWindow` 标题 `Windows 默认锁屏界面`，全程复检）；`WindowsKeyboard.java`（本地 sources jar，事件驱动 + 失焦释放注释）；`SendMessageTimeoutW(WM_NULL)` 返回 1（客户端线程存活）。LWJGL native WndProc 焦点门源码无本地来源，`UNKNOWN`。
4. **验证**：实际执行——`./gradlew --no-daemon runClient`（后台，成功进入主菜单）；Python ctypes 注入脚本 ×4（stdin 执行，结果见 6.2 表）；PowerShell `AppActivate`（返回 True，前台未变）；`SendMessageTimeoutW(WM_NULL)`（返回 1）；`rg -n "Reloading ResourceManager"` latest.log/debug.log（各仅 1 条）；`taskkill //PID 4420 //F`（成功）。未执行：解锁会话后的注入重测（无凭据、不越权）；人工操作员按键（未指派）。
5. **剩余风险**：阻塞点为会话锁定；解锁后 keybd_event/SendKeys 路径预期可用但**仍需实验验证**，不得按推测写成可行；锁屏持前台期间任何注入方案（含 Robot）均不可行；LWJGL native 焦点门机制 UNKNOWN（不影响「未触发」结论，仅影响机制归因）；本机无 pyautogui/autohotkey，解锁后若需 GUI 自动化可复用现有 python ctypes/pwsh 能力，不新增工具。下一步：解锁会话后重跑本实验，或指派人工操作员按 F3+T。
6. **复核**：未完成独立复核（本记录为运行验证代理产出，待主代理指派非作者复核后更新本栏）。
