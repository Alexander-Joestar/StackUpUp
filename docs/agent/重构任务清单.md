# 重构任务清单

> 个人玩具项目的进度备忘。`AGENTS.md` 是规范来源，本清单不替代源码、测试输出和运行报告，也不重复那里的不变量。
> 更新基线：2026-09。工作副本中的其他改动均受保护，本次只修改本文件。

## 已完成（有记录可查）

| 范围 | 状态 | 证据入口 |
| --- | --- | --- |
| P0 事实台账、T2a/T2b+T11、T3、T10 | `PASS`（既有记录） | `compatibility-decision-record.md` §3.5；`t2a-容量站点登记表.md` §2.4 |
| T4a/T4b、T5、T6、T7.0/T7.2/T7.3 | `PASS`（既有记录） | CDR §2、§6；DSL 快照与缓存键边界 |
| T9-R1/R3、T14.0 版本与路径 | `PASS`（既有记录） | CDR §8.6、§8.9；`gradle/libs.versions.toml` |
| T8.1 自有不可变译表 | `PASS`（既有记录） | `src/main/kotlin/io/alexjoest/stackupup/LocalizedMessages.kt`、`src/test/kotlin/io/alexjoest/stackupup/LocalizedMessagesTest.kt` |

`PASS` 一律引自既有记录，不是本次复核结论；本次未做独立复核。

## 待办（仍未闭合）

| 范围 | 状态 | 卡在哪 |
| --- | --- | --- |
| T8.0 真实 F3+T 前后对比 | `PARTIAL` | 只做过客户端可启动与注入可行性，真实按键重载未做（`t8.0-真实f3t基线记录.md`） |
| T8.2 重载重新注入 | `BLOCKED` | 等 T8.0 真实基线；不得用 `LanguageMap.replaceWith` 冒充 F3+T |
| T13 覆盖面收缩与回扩 | `BLOCKED` | 容量与真实写入证据未齐；第三方无源码项保持不可判定 |
| T15 build-logic | `PARTIAL` | 结构已落地，「验证已通过」尚未单独成立（见 `build-logic/`） |
| T16 ForgeDevEnv 对齐 | `UNKNOWN` | 未开始；依赖、wrapper 和发布项都没动 |

## 已放弃或降级（不要再当门槛）

- T12 方案 A 已归档，不再执行，也不作为 T13 依赖（CDR §3.8）。禁止恢复审计器、JSONL 守恒报告或写入后补偿。
- T14.1–T14.6 是当时审计留下的记录，不是待办，也没有验收门槛；没查清的部分如实留在各文件里。
- T14.7（`t14.7-发布前矩阵.md`）是已知限制清单，不是准入门；T13 不依赖它。

## 证据入口

> 本节路径除裸文件名（在 `docs/agent/` 内）外均为仓库根相对路径。

- 决策与事实源：`docs/agent/compatibility-decision-record.md`（容量 §3.5、`resolveInventoryClampLimit` 调用面 §2/§6、T12 归档 §3.8、MixinBooter 版本 §8.6/§8.9、装载入口 §8.8）。
- 容量站点：`docs/agent/t2a-容量站点登记表.md`；原版目标表：`docs/agent/t2b-原版目标表.md`。
- 真实客户端基线：`docs/agent/t8.0-真实f3t基线记录.md`；开发自动验收：`docs/runServer-自动化回归.md`。
- 未闭合项汇总见 `docs/agent/START_HERE.md`。
