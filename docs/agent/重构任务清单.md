# 重构任务清单

> 个人玩具项目的进度备忘。`AGENTS.md` 是规范来源，本清单不替代源码、测试输出和运行报告，也不重复那里的不变量。
> 更新基线：2026-09。工作副本中的其他改动均受保护，本次只修改本文件。
> 路径约定：裸文件名（如 `t2a-容量站点登记表.md`）指 `docs/agent/` 下的文档；`AGENTS.md` 指仓库根的规范文件；其他路径（含 `docs/`、`src/`、`gradle/` 开头）均为仓库根相对路径。

> 结构变更说明（旧引用兼容）：本清单原先的「当前状态表」「待办要点」「依赖与历史（压缩）」「依赖 DAG」四节已合并为下方三段表，旧名不再作为现存章节，外部文档按旧名引用时对应如下。
> - 「当前状态表」（或「状态表」）→ 「已登记范围与状态」表 + 「待办」表。
> - 「待办要点」→ 其中的不变量已由 `AGENTS.md` 承载；按主题分组的 `容量与规则基线`、`T8 本地化`、`T9/T14 版本与 Mixin`、`T12/T13`、`T15/T16` 的当前状态见「待办」表与「已放弃或降级」。
> - 「依赖与历史（压缩）」「依赖 DAG」（简写「DAG」）→ DAG 概览已删除，先后关系改由各产物文档自述；历史决策见 `compatibility-decision-record.md`。
> 本清单不再按节号或节点名引用。

## 已登记范围与状态（本清单不代行复核）

本节按产物文档自述与当前直接证据登记各项状态（已落地 / `PARTIAL` / 未实现 / `UNKNOWN`），不在本文档内宣布 `PASS`：按 `AGENTS.md`，`PASS` 要求非作者独立复核，本清单是备忘，不构成也不代替该复核。

| 范围 | 状态（产物文档自述或当前直接证据） | 证据入口 |
| --- | --- | --- |
| P0 事实台账 | 源码事实已登记；记录所在文档整体仍标 `PARTIAL` | `t2a-容量站点登记表.md` §2.4（P0 事实 c）、§2.3（P0 事实 a）、§5（P0 基线） |
| T2a 容量站点登记表 | `PARTIAL`（独立复核待指派，未完成前不得宣称 `PASS`） | `t2a-容量站点登记表.md:4`、§2.4 |
| T2b+T11 原版目标表 | `PARTIAL`（独立复核待指派，未完成前不得宣称 `PASS`） | `t2b-原版目标表.md:4` |
| T3、T10 | 判定与实现完成；记录所在文档整体仍标 `PARTIAL` | CDR §3.5、§3.6 |
| T4a/T4b | 源码中已落地（`VanillaInventoryWriteMixin` 随 T4a 删除、内容键缓存替代 mark/consume 实例身份） | CDR §2；`src/main/kotlin/io/alexjoest/stackupup/StackLimitHooks.kt:18`、`:215`（T4b 内容键缓存）、`mixin-生态与注入最佳实践.md:262` |
| T5 matcher 表达式树 | 源码与测试中已落地；未见独立复核记录 | `src/main/kotlin/io/alexjoest/stackupup/rules/field/FieldMatcher.kt`；`src/test/kotlin/io/alexjoest/stackupup/rules/field/FieldMatcherTest.kt:15` |
| T6 缓存键由 matcher 树机械推导 | 源码与测试中已落地；未见独立复核记录 | `src/main/kotlin/io/alexjoest/stackupup/rules/compile/RuleContextRequirements.kt:12`、`src/main/kotlin/io/alexjoest/stackupup/rules/RuleLanguage.kt:14` |
| T7.0 真实 token 位置 | `PARTIAL` | token 已带列号（`src/main/kotlin/io/alexjoest/stackupup/rules/parse/DslToken.kt:64`），但列号目前只用于 item 字面量错误（`src/main/kotlin/io/alexjoest/stackupup/rules/parse/DslParser.kt:198`）；其余 DSL 错误仍无列号（`docs/DSL-v2-规则示例.md:328`） |
| T7.1 两段式 tokenizer | 未实现（规划项） | 现行 tokenizer 是单趟扫描、未见按 `->` 分段的两段式实现（`src/main/kotlin/io/alexjoest/stackupup/rules/parse/DslTokenizer.kt`）；`docs/DSL-v2-规则示例.md:325-327` 亦明确其仍未实现 |
| T7.2 literal grammar / fail-fast / `@*` / 多冒号 | `PARTIAL`（源码与测试有直接证据，但所引说明文档仍列为规划项，且未见独立复核） | `src/main/kotlin/io/alexjoest/stackupup/rules/parse/ItemLiteralSyntax.kt:6`、`:33`；`src/test/kotlin/io/alexjoest/stackupup/rules/RuleLiteralMatcherTest.kt:95`；文档仍标规划见 `docs/StackUpUp-实现与兼容性说明.md:206` |
| T7.3 专用 `RangeConditionAst` | `PARTIAL`（源码与测试有直接证据，但所引说明文档仍列为待实现，且未见独立复核） | `src/main/kotlin/io/alexjoest/stackupup/rules/ast/RangeConditionAst.kt`；`src/main/kotlin/io/alexjoest/stackupup/rules/parse/DslParser.kt:165`；`src/test/kotlin/io/alexjoest/stackupup/rules/DslParserTest.kt:70`；文档仍标待实现见 `docs/DSL-v2-规则示例.md:329` |
| T9-R1/R3、T14.0 版本与路径 | 决策与账本已形成；记录所在文档整体仍标 `PARTIAL` | CDR §8.6、§8.9；`gradle/libs.versions.toml` |
| T8.1 自有不可变译表 | 实现与测试存在 | `src/main/kotlin/io/alexjoest/stackupup/LocalizedMessages.kt`、`src/test/kotlin/io/alexjoest/stackupup/LocalizedMessagesTest.kt` |

## 待办（仍未闭合）

| 范围 | 状态 | 卡在哪 |
| --- | --- | --- |
| T8.0 真实 F3+T 前后对比 | `PARTIAL` | 真实判据已出现：外部操作者曾触发第二次 `Reloading ResourceManager`（r9，10:03:17），但触发者归属与可重复性 `UNKNOWN`，本任务自动化注入 1–10 无一成功（`t8.0-真实f3t基线记录.md:117` §6.5、`:141` §6.9） |
| T8.2 重载重新注入 | `BLOCKED` | 等 T8.0 真实基线；不得用 `LanguageMap.replaceWith` 冒充 F3+T |
| T13 覆盖面收缩与回扩 | `BLOCKED` | 容量与真实写入证据未齐；第三方无源码项保持不可判定 |
| T15 build-logic | `PARTIAL` | 结构已落地，「验证已通过」尚未单独成立（见 `build-logic/`） |
| T16 ForgeDevEnv 对齐 | `UNKNOWN` | 未开始；依赖、wrapper 和发布项都没动 |

## 已放弃或降级（不要再当门槛）

- T12 方案 A 已归档，不再执行，也不作为 T13 依赖（CDR §3.8）。禁止恢复审计器、JSONL 守恒报告或写入后补偿。
- T14.1–T14.6 是当时审计留下的记录，不是待办，也没有验收门槛；没查清的部分如实留在各文件里。
- T14.7（`t14.7-发布前矩阵.md`）是已知限制清单，不是准入门；T13 不依赖它。

## 证据入口

> 本节路径除裸文件名（在 `docs/agent/` 内）外均为仓库根相对路径。

- 决策与事实源：`docs/agent/compatibility-decision-record.md`（容量 §3.5、`resolveInventoryClampLimit` 调用面 §2/§6、T12 归档 §3.8、MixinBooter 版本 §8.6/§8.9、装载入口 §8.8）。
- 容量站点：`docs/agent/t2a-容量站点登记表.md`；原版目标表：`docs/agent/t2b-原版目标表.md`。
- 真实客户端基线：`docs/agent/t8.0-真实f3t基线记录.md`；开发自动验收：`docs/runServer-自动化回归.md`。
- 未闭合项汇总见 `docs/agent/START_HERE.md`。
